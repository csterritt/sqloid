## sqloid

A terminal-based structured query builder and data browser for SQLite and local D1 databases — build SELECT/INSERT/UPDATE/DELETE
queries through guided fields instead of typing SQL, then view, page through, and export results as CSV/JSON/SQL.
