# Sqloid Wiki Index

Content-oriented catalog of all wiki pages.

## Project

- [project-overview.md](project-overview.md) — high-level description of Sqloid, its stack, and module architecture.

## Source code

- [source-code.md](source-code.md) — catalog and summaries of all source files under `cmd/` and `internal/`.

## Concepts

- [cli-contract.md](cli-contract.md) — the full command-line contract: commands, flags, stream ownership, exit statuses, and silent successful startup.
- [d1-discovery.md](d1-discovery.md) — Issue #3 local D1 candidate discovery: the exact Wrangler directory, case-sensitive `.sqlite` rule, lowercase `metadata` and `-wal`/`-shm` exclusions, exact zero/multiple-candidate failure diagnostics (Issue #4), cardinality outcomes, and the `internal/d1` → `internal/cli` → `internal/connection` handoff.
- [sqlite-startup.md](sqlite-startup.md) — Issue #2 startup pipeline: pre-open validation order, failure classes, one-line diagnostics, mode=rw opening, schema probe, journal-mode preservation, no creation/modification, and no read-only fallback.
- [connection-pool.md](connection-pool.md) — Issue #5 exact-two pool and dedicated leasing: minimum/maximum pool size of two, lease lifecycle and release safety, five-second busy timeout on every connection, exact 64 MiB connection-local SQLITE_LIMIT_LENGTH, and unchanged WAL/rollback-journal modes.
- [cancellation-infrastructure.md](cancellation-infrastructure.md) — Issue #6 cancellable request lifecycle: unique request identity, context ownership, idempotent Cancel with exactly-one connection-scoped interrupt dispatch, cancelling-until-settlement visibility, cancellation-wins late-success classification, lease release only after settlement, no force-close with safe reuse, the one-second CPU and five-second lock-wait bounds on Linux/macOS, and the modernc v1.57.0 assumptions these depend on (SELECT/write/UI wiring deferred to Issue #28).
- [session-health.md](session-health.md) — Issue #7 request-boundary database identity checks: startup device/inode recording on Linux/macOS, verification before every request and before any newly opened/replacement pooled connection is admitted for use, typed `HealthDeleted`/`HealthReplaced` outcomes with preserved causes, rename-away as absence, same-inode in-place mutation as ordinary SQLite behavior, post-error reclassification precedence, successful-result-stands race handling, exactly one pre-BEGIN check per phased write, no watcher/polling/UI dependency, and terminal copy deferred to Issue #46.
- [schema-catalog.md](schema-catalog.md) — Issue #9 schema catalog and table eligibility: main-schema-only queries (`main.sqlite_master`, bound-parameter `main.pragma_table_xinfo(?)`, same-request `PRAGMA schema_version` capture), ordinary/virtual/view kinds, write eligibility (views SELECT-only), rowid capability and declared-rowid alias shadowing, `WITHOUT ROWID` detection, fts5 shadow tables as ordinary tables, hidden/generated column noninsertability, declared type as metadata only, exact `sqlite_%` and `_cf_METADATA` exclusions, deterministic name ordering, and strict UI/driver/querybuilder independence.

- [responsive-tui-shell.md](responsive-tui-shell.md) — Issue #8 responsive Bubble Tea shell: pinned Bubble Tea/Lip Gloss versions, one-row global footer, builder desired border/padding-inclusive height capped at floor(H/3), all remaining rows to an independently bordered results region exceeding half-height, complete-row page-area arithmetic, focused-field internal scrolling, the exact 80×24 minimum, exact `terminal too small` suspension with ignored input, conditional Ctrl+W cancellation routing, and exact state restoration on resize.
- [early-integration-tracer.md](early-integration-tracer.md) — Issue #10 disposable early integration tracer: hardcoded safely quoted `SELECT *` flowing Schema → Connection → UI, typed `TracerResult`/`TraceResult` row and error boundaries, minimal bordered grid rendering inside the Issue #8 shell, deliberately isolated state, every omitted production feature (builder, validation, paging, count, history, cancellation UX, recovery, writes), and the mandatory replacement of the tracer rather than extension by Issue #22.

- [builder-command-table.md](builder-command-table.md) — Issue #11 builder startup lifecycle: exact idle prompt with no result-only headers, initial Command focus, one-key S/U/D/I selection/replacement, Table advancement, immutable transitions with downstream clearing, Schema-owned eligibility (ordinary/virtual tables writable, views SELECT-only), view-to-write clearing, revisiting Command, and resulting focus transitions.
- [searchable-popups.md](searchable-popups.md) — Issue #12 searchable popup interaction contract: reusable searchable versus scroll-only candidate lists with identity/display separation and source-order preservation, case-insensitive subsequence matching, exact `no matches` empty/no-match states kept open, deterministic highlight/viewport reset on every search change, clamped boundary scrolling and viewport windows, popup-row search-input modality without builder-key leakage, single-select acceptance versus multi-select add-and-reopen with duplicate exclusion, Esc preservation of completed multi-selections, exact opener focus restoration on both paths, Table as the first end-to-end searchable single-select consumer committing identity through QueryBuilder transitions, and overlay rendering without region reflow.
- [stale-schema-refresh.md](stale-schema-refresh.md) — Issue #13 stale schema refresh: refresh-before-presentation on every Table-popup open through the typed `CatalogRefresher` seam with per-attempt identities, unchanged prior-catalog retention on ordinary failure with the exact persistent `Schema data is stale — retry or cancel` status plus inline `could not refresh: <cause>`, blocked acceptance/continuation/execution while stale, retry (duplicate-gated, indicator-preserving) and cancel (exact pre-open restoration) lifecycle, repeated-failure cause updates, and typed deletion/replacement health classifications taking terminal precedence (`Database file no longer exists/was replaced — session ended`) over stale state and late results.

## Tests

- [unit-tests.md](unit-tests.md) — catalog and summaries of the Go unit and process-boundary tests.
- [sql-atoms-and-literals.md](sql-atoms-and-literals.md) — Issue #14 universal value parsing (verbatim INTEGER-first then finite-REAL grammar with exact TEXT fallback), concrete SQLite bind types with no declared-type coercion, schema-only atom-by-atom identifier quoting, closed operator/aggregate/direction choices, and the canonical standalone INTEGER/REAL/TEXT/NULL/BLOB literal renderer owned by `internal/querybuilder/sql_literal.go` for Issues #40/#48.
- [projection-count-star.md](projection-count-star.md) — Issue #15 SELECT wildcard and COUNT(*) projection path: identity-separate wildcard/sentinel/named-column candidates with collision-free key encodings, empty-projection ordering (`*` default-highlighted first, bare `COUNT(*)` second, visible columns after), conditional sentinel visibility with reappearance when the projection empties, direct sentinel commits reopening Column(s), named-column routing through the scroll-only Value/Count/Min/Max/Avg/Sum aggregate popup, and aggregate-on-wildcard unrepresentable by construction.
- [where-guided-predicates.md](where-guided-predicates.md) — Issue #17 guided WHERE predicates and SQL NULL semantics: structurally distinct absent/column-chosen/awaiting-value/complete states, the closed nine-operator set without type filtering, immediate no-value/no-parameter `IS NULL`/`IS NOT NULL` completion, builder-owned committed predicate plus revision drafts restoring exact prior choices, typed `NULL` and empty input as verbatim TEXT, ordinary comparison/LIKE SQL-null behavior preserved, LIKE `%`/`_` bound byte-for-byte, inline hint plus contextual help, Esc restoration of prior completions, and the shared SELECT/UPDATE/DELETE rendering contract.
- [group-order-limit.md](group-order-limit.md) — Issue #18 GROUP BY, ORDER BY, and LIMIT: assisted multi-selection with acceptance-order groups, duplicate/stale no-ops, and the complete grouping validity matrix (mixed, all-aggregate, bare COUNT(*), wildcard exclusivity); context-valid ORDER BY candidates (grouped columns plus selected aggregates only), one-expression ownership, ASC default, DESC toggling; bounded LIMIT accepting 1 through 9223372036854775807 with entered-text preservation and the exact `Limit must be an integer from 1 to 9223372036854775807` reason; and the SELECT field bar's Group By / Order By / Limit wiring.
- [projection-ordered-editing.md](projection-ordered-editing.md) — Issue #16 ordered projection editing: insertion-order entries across Value/Count/Min/Max/Avg/Sum with same-column distinct aggregates, exact-pair duplicate rejection as full no-ops, bare `COUNT(*)` coexistence and defensive duplicate-sentinel no-ops, wildcard atomic replacement with sole-entry exclusivity blocking appends until removal, immutable transitions, Backspace/Delete removing exactly the latest entry from the base Column(s) field with empty no-op and Issue #15 candidate restoration, and popup-context key scope.
