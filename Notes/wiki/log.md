# Wiki Log

## [2026-08-26] ingest | Issue #1 CLI shell and usage contracts

Initial wiki setup and ingest of the Issue #1 implementation: created `project-overview.md`, `source-code.md`, `cli-contract.md`, and `unit-tests.md`; documented the `mow.cli` command structure, the `sqlite <file>` and `d1` commands, help/version flags, exact stream ownership, exit statuses (0/2), silent successful startup, and the `internal/cli` vs `cmd/sqloid` roles. Updated `index.md`.
## [2026-08-26] ingest | Issue #2 SQLite startup validation and read-write errors

Ingested the Issue #2 implementation: pinned `modernc.org/sqlite v1.57.0`; created `internal/connection` (`Open`, `Session`, structured `StartupError` classes) with ordered non-mutating pre-open validation (existence → readability → exact header → writability proof → mode=rw open → schema probe); connected the sqlite handler in `internal/cli` to render exact one-line diagnostics with status 1 while `cmd/sqloid` stayed a thin entrypoint. Added [sqlite-startup.md](sqlite-startup.md); updated `cli-contract.md`, `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-26] ingest | Issue #3 local D1 candidate discovery

Ingested the Issue #3 implementation: created `internal/d1` (`Discover`, exact-rule `eligible` filter, typed `ErrNoCandidate`/`ErrMultipleCandidates` sentinels) scanning only `.wrangler/state/v3/d1/miniflare-D1DatabaseObject` non-recursively; wired `internal/cli.RunD1` to pass the sole candidate unchanged to `internal/connection.Session` with no D1-specific opening path; fixed `mustFileURL` so relative paths render as opaque `file:` URIs (no invented URI authority). Created [d1-discovery.md](d1-discovery.md); updated `cli-contract.md`, `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-26] ingest | Issue #4 exact D1 discovery diagnostics

Ingested the Issue #4 implementation and golden tests: `internal/cli.mapDiscoveryDiagnostic` now maps typed `internal/d1` outcomes to exact process diagnostics — zero candidates (missing, unreadable, empty, or candidate-free Wrangler directory) emit exactly two stderr lines (`no candidate database found in .wrangler` plus the expected-path and `sqloid sqlite <file>` explicit-open hint), multiple candidates emit only `There is more than one SQLite database in .wrangler` with no hint; both bypass the opener, exit 1, and create nothing. Added discovery-failure section to [d1-discovery.md](d1-discovery.md); updated `cli-contract.md`, `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-26] ingest | Issue #5 two-connection SQLite pool and limits

Ingested the Issue #5 implementation: `internal/connection` now opens an exact-two pool (minimum and maximum both 2), applies the five-second busy timeout to every physical connection via the DSN `_busy_timeout` parameter, and adds dedicated leasing (`DB.Lease`, `Lease.Conn`, `Lease.Release`) that configures each leased connection with the exact 64 MiB connection-local SQLITE_LIMIT_LENGTH through `modernc.org/sqlite`'s documented `sqlite.Limit` mechanism before any work runs. Barrier-driven tests prove concurrent callers receive distinct physical connections in pre-placed WAL and rollback-journal fixtures with all per-connection configuration intact, journal mode never set or changed, release cleanup safe on success/error/repeat, and released leases refused for reuse. Created [connection-pool.md](connection-pool.md); updated `sqlite-startup.md`, `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-26] ingest | Issue #6 cancellation infrastructure

Ingested the Issue #6 implementation: `internal/connection/request.go` adds the reusable cancellable request lifecycle (`Request` with process-unique atomic-counter identity, owned derived context, idempotent `Cancel` dispatching exactly one connection-scoped interrupt via the nilable `Lease.interruptFn` seam, visible `running`/`cancelling`/`settled` state persisting until exactly-once `Settle`, cancellation-wins late-success classification, lease release only after settlement through idempotent `Close`, never force-closing a connection). Fake-backed tests cover identity, racing-Cancel interrupt uniqueness, state visibility, the full outcome matrix, and real-pool lease-hold-before-settlement with no-force-close/reuse. Pinned-driver barrier-based capability tests (`interrupt_unix_test.go`, Linux/macOS mandatory and release-blocking) prove modernc v1.57.0 interrupts settle controlled CPU-bound work within one second and SHARED-lock-blocked writes within five seconds, isolate the other dedicated lease, discard deliberately released late success as cancelled, and leave the interrupted connection usable and reusable. Created [cancellation-infrastructure.md](cancellation-infrastructure.md); updated `connection-pool.md` indirectly via cross-references, `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-26] ingest | Issue #7 request-boundary database identity checks

Ingested the Issue #7 implementation and Linux/macOS tests: `internal/connection/health.go` adds typed `HealthKind`/`HealthError` (`HealthDeleted`, `HealthReplaced`) with neutral diagnostics, `VerifyHealth()` comparing the startup-recorded device/inode of the validated target (recorded in `Open`; platform split via `identity_unix.go`/`identity_other.go`), and `RunRequest(parent, op)` as the reusable request boundary — verification inside `Lease` before every request and before any newly opened/replacement pooled connection is admitted for use; post-error reclassification makes deletion/replacement take precedence over ordinary error handling while successful results stand until the next boundary; in-place same-inode mutation stays ordinary SQLite behavior. `Lease` wiring updated in `startup.go`. Barrier-driven `health_test.go` proves classification tables, lease refusal, boundary-before-work blocking with exactly one check, raced replacement followed by error/success, and exactly one pre-BEGIN check for entire phased writes (commit and rollback) with none between statement execution and COMMIT; no watcher/polling/UI dependency and terminal copy remains Issue #46's. Created [session-health.md](session-health.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #8 responsive TUI shell and minimum-size restoration

Ingested the Issue #8 implementation: created `internal/ui` with pinned `bubbletea v1.3.6`/`lipgloss v1.1.0` (no Bubbles components apply to the shell yet). Documented exact region arithmetic (one bottom global footer row; builder desired height including its own border+padding capped at floor(H/3); every remaining row to an independently bordered results region greater than half-height; complete-row page area after results-owned border, status/count, and header rows), focused-field internal scrolling within the capped viewport, the exact 80×24 minimum where `View()` returns exactly `terminal too small`, hidden-state suspension without mutation, ignored ordinary keys, Ctrl+W routing only when hidden state owns active cancellable work, and exact context/focus restoration on return to supported dimensions. Created [responsive-tui-shell.md](responsive-tui-shell.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #9 schema catalog and table eligibility

Ingested the Issue #9 implementation: created `internal/schema` (`schema.go` types + `catalog.go` pure `BuildCatalog` rules) and `internal/connection/schema.go` plumbing through the shared request boundary. Documented main-schema-only queries (`main.sqlite_master` with `type IN ('table','view') ORDER BY name`, bound-parameter `main.pragma_table_xinfo(?)`, same-request `PRAGMA schema_version` capture), ordinary-table/virtual-table/view kinds, write eligibility with SELECT-only views, rowid capability {has-rowid, without-rowid, not-applicable}, declared-rowid-alias shadowing, suffix-based WITHOUT ROWID detection, fts5 shadow tables as ordinary tables, hidden/generated column noninsertability from table_xinfo raw values, declared type as metadata only (no type-specific input behavior), exact `sqlite_%` and `_cf_METADATA` exclusions, and deterministic ascending-name output; refresh failures keep typed health classification and preserve causes for stale-data/retry UI use. Created [schema-catalog.md](schema-catalog.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #10 early integration tracer

Ingested the Issue #9-of-#10 disposable tracer implementation: `internal/schema/tracer.go` (`ChooseTracerTarget` typed catalog selection plus `SelectAllSQL` safely double-quoted hardcoded `SELECT *`), `internal/connection/tracer.go` (`RunTraceSelectAll` executing exactly one quoted SELECT * as one complete `RunRequest`, `TracerResult{Columns, Rows [][]any}` typed transport with preserved-cause neutral failures), and the Bubble Tea composition path (`internal/ui/tracer.go` messages/executor seam with isolated `Model.Trace *TraceView`; rendering in `internal/ui/view.go`). Documented the risk-reduction purpose, Connection–Schema–UI ownership without UI database behavior, typed row/error boundaries (nil/int64/float64/string/[]byte), minimal bordered grid/basic error display inside the Issue #8 shell, and every omitted production feature — builder, validation, pre-execution workflows, WHERE/GROUP BY/ORDER BY/LIMIT, parameter binding, paging, count request, cache, histories, cancellation UX, retry/recovery, and writes. Stated explicitly that Issue #22 must replace the tracer rather than extend it into a production query path; referenced Issue #10 and the Module Design and Testing Decisions in `Notes/PRD-sqloid.md`. Created [early-integration-tracer.md](early-integration-tracer.md); updated `source-code.md`, `unit-tests.md`, and `index.md`. No composition-root change was needed: `cmd/sqloid/main.go` is untouched because no live TUI launch exists yet.
## [2026-08-27] ingest | Issue #11 command and table selection lifecycle

Ingested the Issue #11 implementation: created `internal/querybuilder` (`builder.go` package scope + `command_table.go` lifecycle) with immutable value-level transitions — `NewQuery()` starts unselected with Command focused; each one-key S/U/D/I replacement bumps `DownstreamGeneration()` (all downstream command-specific state discarded), recomputes eligibility from `internal/schema` metadata (`WriteEligible`/kinds, no duplicated catalog rules), retains a selected table only while still eligible for the new command, clears names absent from the latest refresh, and focuses Table; views survive SELECT only and clear for every write command while the refreshed eligible write-table list stays populated with ordinary and virtual tables. Integrated through `internal/ui/command_table.go` (+ `QB` field, `SchemaRefreshedMsg{Catalog}` injection in `model.go`): Command renders at startup, plain letters route only while Command is focused, Shift+Tab revisits Command for replacement, Table appears and holds focus after any selection. Idle results region pinned to exactly `Select a command (S/U/D/I) to begin` with no frozen header/range/count, unchanged layout arithmetic at 80×24/100×30/160×50, and distinction from executed-empty presentation. Red-green test flow documented (pure builder tests + scripted model tests + rendering assertions); tracer remains isolated pending wholesale replacement by Issue #22; searchable popups deferred to Issue #12. Cross-referenced Issue #11 and the Builder and Display Interaction, Builder lifecycle, QueryBuilder Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [builder-command-table.md](builder-command-table.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #12 searchable popup interaction contract

Ingested the Issue #12 implementation: created `internal/ui/popup.go` (reusable searchable/scroll-only popup state: identity-separate candidates, source-order preservation, case-insensitive subsequence matching, exact `no matches` empty/no-match states kept open, deterministic highlight/viewport reset on every search-text change, clamped boundary scrolling, viewport windows, single-select accept, multi-select add-and-reopen with duplicate exclusion and insertion-order `Completed()`), `internal/ui/popup_view.go` (deterministic rendering of the search-input line, highlighted/plain rows, `no matches`, viewport windowing, go-runewidth cell truncation, and `composeOverlay` splicing that never reflows base regions), and the model routing in `popup.go`/`model.go` (`installPopup` capturing exact opener focus, `handlePopupKey` consuming Up/Down/Enter/Esc/printables before base context so nothing leaks into builder shortcuts, close-then-commit accept hooks). Wired Table through `internal/ui/table_popup.go` as the first end-to-end searchable single-select consumer: fresh popups from `QueryBuilder.EligibleTables()` under the selected command's own eligibility rules, acceptance committing object identity via `QB.SelectTable` plus `applyBuilder`, viewport capped at 8 rows, Enter/Esc both restoring the exact opener focus. Documented variants, matching, reset, scrolling, input modality, selection/duplicate/Esc semantics, restoration, rendering, and later column/grouping/aggregate/operator flows as future consumers. Referenced Issue #12 and the Builder and Display Interaction, UI Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [searchable-popups.md](searchable-popups.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #13 stale schema refresh, retry, and terminal precedence

Ingested the Issue #13 implementation: `internal/schema/refresh.go` (typed `Attempt`/`RefreshStatus` with constructor+`Valid()` payload rules; terminals mapped from Connection `HealthError` kinds) and `internal/ui/schema_refresh.go` (`CatalogRefresher` seam invoked only in tea.Cmd, identified `SchemaRefreshSettledMsg`, per-open `issueRefresh`, retry/cancel messages, attempt-identity discards of superseded/late/post-terminal results). Every Table open now issues one fresh catalog request through `beginTablePopup`; ordinary failures retain prior catalogs verbatim behind exactly `Schema data is stale — retry or cancel` plus inline `could not refresh: <cause>` (rendered once per surface — popup body while open, results region after close), block acceptance/navigation/continuation via `ContinuationBlocked()`; Esc under stale cancels the flow restoring exact opener/pre-open state; repeated failure updates only that attempt's cause leaving retry/cancel available; successful retry installs the refreshed catalog and continues the popup via deterministic whole-catalog `Popup.ReplaceCandidates`; typed deletion/replacement transitions to exact `Database file no longer exists/was replaced — session ended` shells, suppressing all controls and rejecting late completions. Created [stale-schema-refresh.md](stale-schema-refresh.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.

## [2026-08-28] ingest | Issue #14 universal value parsing and safe SQL atoms

Ingested the Issue #14 implementation: `internal/querybuilder/value.go` (verbatim untrimmed `ParseValue` grammar — INTEGER `-?[0-9]+` fitting signed int64 first including leading zeros and both boundaries, finite `strconv.ParseFloat` REAL second including hex floats with leading-`+` and overflow fall-through, exact TEXT fallback for whitespace/NUL/typed NULL/nonfinite/hex-integer/injection shapes; `ParamValue()` stable `int64`/`float64`/string bind types with typed NULL and empty input staying strings, no declared-type coercion), `sql_atoms.go` (single-atom double-quote identifier quoting fed only by `internal/schema` object/column identities, closed Operator/Aggregate/Direction enums rendering exact PRD tokens and rejecting invalid values, Predicate assembly binding every user value with deterministic ordering), and `sql_literal.go` (`RenderSQLLiteral`: canonical decimal INTEGER, shortest round-trip REAL with `.0` restoration preserving integral/negative-zero/subnormal/exponent identity and typed non-finite rejection, quote-doubled TEXT preserving emptiness/whitespace/NUL bytes, exactly `NULL`, `X'lowercase-hex'` BLOB — the sole serializer exposed to future Issues #40 and #48 while execution stays on bound parameters). Table-driven red-green tests: `value_test.go`, `sql_atoms_test.go`, `sql_literal_test.go`. Cross-referenced Issue #14 and the Query Grammar, Numeric value parsing and rendering, SQL safety, Query save targeting, and Testing Decisions of `Notes/PRD-sqloid.md`. Created [sql-atoms-and-literals.md](sql-atoms-and-literals.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.

## [2026-08-27] ingest | Issue #15 SELECT wildcard and COUNT(*) projection path

Ingested the Issue #15 implementation: `internal/querybuilder/projection.go` (typed projection candidates with identity separate from display text and reserved-prefix `Key()` encodings so real columns named `*`/`COUNT(*)` never collide with synthetic identities; empty-projection derivation with `*` first/default-highlighted, bare `COUNT(*)` second, then visible columns in Schema order; conditional sentinel hiding once any entry exists and reappearance when removal empties the projection; immutable transitions where wildcard and bare `COUNT(*)` commit directly — sentinel requesting a Column(s) reopen without ever asking for a named-aggregate choice — while named columns return pending aggregate choices that `CompleteProjectionAggregate` turns into `(column, aggregate)` entries with reopen; guard-rejected out-of-range removals) integrated into `builder.go`/`command_table.go` (projecting state on `QueryBuilder`, cleared on command replacement and table-loss refresh, plus the new `FieldColumns`). UI side: `internal/ui/projection_popup.go` opens Column(s) as a searchable single-select popup populated verbatim from builder candidates at 8-row viewport, routes acceptance through builder outcomes — direct sentinel/wildcard commits followed by `reopenColumnsPopup` (exact focus, deterministic reset) only when requested, named columns into the scroll-only Value/Count/Min/Max/Avg/Sum aggregate popup whose completion reopens Column(s) preserving completed entries — and renders committed entries in the new field-bar slot via `command_table.go`; `table_popup.go` routes Enter on focused Column(s). Documented that wildcard and bare `COUNT(*)` never open an aggregate popup and that `MIN(*)`/`MAX(*)`/`AVG(*)`/`SUM(*)` are unrepresentable by construction. Referenced Issue #15 and the Query Grammar, Builder and Display Interaction, QueryBuilder Module Design, UI Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [projection-count-star.md](projection-count-star.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #16 ordered projection editing and deduplication

Ingested the Issue #16 implementation: `internal/querybuilder/projection.go` (`projectionHasWildcard` guarding exclusivity; `AcceptProjection` accepting wildcard from any state as an atomic whole-list replacement focused on Column(s) with no reopen, sentinel re-commits as unchanged no-ops, junk-carrying synthetic identities normalized, malformed empty-name named identities routing nowhere; `CompleteProjectionAggregate` rejecting aggregate completion beside a wildcard and exact repeated `(column, aggregate)` pairs — including `(column, Value)` — as full no-ops without reorder/replace/focus change while distinct appends continue in insertion order across Value/Count/Min/Max/Avg/Sum with same-column distinct aggregates and same-aggregate distinct columns, the bare `COUNT(*)` sentinel coexisting with later named aggregates; new immutable `RemoveLatestProjection()` removing exactly the latest committed entry per call, walking backward through named entries and the sentinel, emptying a sole wildcard, no-op when empty). UI side: `internal/ui/model.go` routes Backspace/Delete from the base Column(s) field (popup-free, non-suspended base context) through that builder transition via `applyBuilder`, keeping focus on Column(s), leaving popup contexts governed by the reusable Issue #12 contract (search backspace edits search only; scroll-only aggregates unaffected), and relying on QueryBuilder candidate derivation so returning to empty restores Issue #15's `*`-first/`COUNT(*)`-second sequence without patching candidates in UI code. Tests: `internal/querybuilder/projection_test.go` (ordered insertion, duplicate rejection, sentinel defense, wildcard atomic replacement/exclusivity, malformed identities, remove-latest contract including rejected-duplicate history) and `internal/ui/projection_editing_test.go` (scripted Backspace/Delete walks through wildcard/named/sentinel states, empty no-op, restoration of empty candidates, and popup-context key scope). Cross-referenced Issues #15/#16 and the Query Grammar, Builder and Display Interaction, keyboard context matrix, QueryBuilder Module Design, UI Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [projection-ordered-editing.md](projection-ordered-editing.md); updated `source-code.md`, `unit-tests.md`, `index.md`, and linked forward from [projection-count-star.md](projection-count-star.md).

## [2026-08-27] ingest | Issue #17 guided WHERE predicates and SQL NULL semantics

Ingested the Issue #17 implementation: `internal/querybuilder/predicate.go` (`WherePredicate` structural stages `WhereAbsent`/`WhereColumnChosen`/`WhereAwaitingValue`/`WhereComplete` with immutable `SelectColumn`/`ChooseOperator`/`SubmitValue` transitions; null operators completing immediately at choice discarding stale values and binding nothing; universal-parse submissions keeping typed NULL, empty input, and LIKE `%`/`_` as verbatim TEXT parameters with byte-exact `Entered()` restoration; `SQL()` rendering quoted atom + fixed token + `'?'` only when complete; QueryBuilder-owned committed predicate plus seeded revision draft via `StartWhere(column)` eligibility validation, `ApplyWhereDraft`/`CancelWhereDraft`/`CommitWhereDraft`, downstream clearing on command replacement/table loss, deterministic `FixedOperators()` without type filtering, visible-column `WhereCandidates()`). UI side: `internal/ui/value_input.go` (verbatim `ValuePrompt` rune buffer + insertion cursor, reversed cursor cell, hook-based Enter/Esc ownership, hint/help/footer line rendering), `internal/ui/where_popup.go` (searchable column popup from builder candidates, scroll-only nine-token operator popup with revision highlight restore, transition-result routing — null ops commit immediately, value ops open the seeded prompt — whole-draft Esc cancel restoring prior completions and exact opener focus, `applyWhereFlow` staged-focus pinning), `internal/ui/model.go` (ValuePrompt key precedence before popup/base handling), `internal/ui/view.go` (non-reflowing prompt overlay composing inline SQL-NULL hint plus comparison/LIKE wildcard help), and `internal/ui/command_table.go` (Where field-bar entry for SELECT/UPDATE/DELETE in Query Grammar order; two exact-field-count assertions legitimately updated to include the new entry). Tests: `predicate_test.go` (full column × operator matrix, typed-NULL TEXT binding end-to-end, quoting, immutability, defensive rejections) and scripted/view pairs `where_flow_test.go` + `where_view_test.go`. Cross-referenced Issues #12/#14/#17 and the Query Grammar, Builder and Display Interaction, Numeric value parsing and rendering, SQL safety, QueryBuilder, UI, and Testing Decisions of `Notes/PRD-sqloid.md`. Created [where-guided-predicates.md](where-guided-predicates.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.

## [2026-08-28] ingest | Issue #18 GROUP BY, ORDER BY, and LIMIT rules

Ingested the Issue #18 implementation: `internal/querybuilder/group_by.go` (assisted GROUP BY multi-selection preserving acceptance order with Schema-derived candidates excluding committed names, exact-duplicate/hidden/stale rejection as immutable no-ops, the complete grouping validity matrix through `FirstInvalidIssue` — mixed aggregate/nonaggregate projections requiring every nonaggregate grouped, missing-group and extra-group cases, all-aggregate and bare `COUNT(*)` validity without GROUP BY, wildcard × GROUP BY exclusivity, stale-group detection — with exact reason wordings), `internal/querybuilder/order_by.go` (typed candidate identities with reserved-prefix keys distinguishing equal labels, context-derived candidates offering only table columns or grouped columns plus selected aggregates plus bare `COUNT(*)`, atomic sole-selection replacement with the ASC default, closed ASC/DESC toggling, whole-value clearing, staleness first-invalid reporting), `internal/querybuilder/limit.go` (verbatim entered representation beside the optional parsed integer, empty = unbounded with no clause, base-10 acceptance in `[1, 9223372036854775807]` rendering canonically, every other category invalid with exactly `Limit must be an integer from 1 to 9223372036854775807` and preserved text), `select_sql.go` (deterministic quoted statement in grammar order with no added parameters), and `validation.go` (fixed-order first-invalid contract). UI wiring: `internal/ui/group_by_popup.go`, `order_by_popup.go`, and `limit_field.go` add the Group By / Order By / Limit field-bar entries in Query Grammar order with commit-and-reopen multi-selection, popup acceptance with exact opener-focus restoration, base-field Backspace removal, Up/Down direction toggling without popup or focus movement, value-entry seeding byte-for-byte with the prior Limit, verbatim invalid-reason display, and no second UI validator. Tests: pure `group_by_test.go`/`order_by_test.go`/`limit_test.go` matrices asserting exact validity, reasons, candidate order, and quoted SQL, plus scripted `group_by_test.go`/`order_by_test.go`/`limit_test.go` in `internal/ui`. Cross-referenced Issues #15–#18 and the Query Grammar, Runnable-State Contract, Builder and Display Interaction, QueryBuilder, UI, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [group-order-limit.md](group-order-limit.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.

## [2026-08-27] ingest | Issue #19 authoritative runnable-state feedback

Ingested the Issue #19 implementation: `internal/querybuilder/runnable.go` (pure UI-independent `QueryBuilder.RunnableReport()` returning runnable or the first invalid typed `RunField` plus one specific reason in each command's visual builder order; common gates — selected command, refreshed identifiers including stale WHERE columns/groups/ordering, no incomplete value prompt; per-command prerequisites reusing the Issue #18 grouping/ORDER BY/LIMIT validators — SELECT nonempty projection, UPDATE unique complete {Value, NULL} SET choices with submitted Values plus optional complete WHERE, DELETE eligible table with optional complete WHERE, INSERT insertable-column blocking with all-omit validity; test-asserted reason constants including the exact PRD `table has no insertable columns` and reused `Limit must be an integer from 1 to 9223372036854775807`), `internal/querybuilder/write_state.go` (typed placeholder UPDATE SET assignment and INSERT per-column {Value, NULL, Default/Omit} seam with submitted-empty-TEXT and SQL-NULL-versus-typed-`NULL` distinctions for Issues #37/#39), and `internal/querybuilder/whole_value.go` (immutable whole-value clearing preserving surrounding structural choices: WHERE column/operator reopened as an incomplete draft, Limit restored to valid unbounded, UPDATE/INSERT Value choices kept while dropping submissions, empty no-ops). UI integration in `internal/ui/runnable_feedback.go` (base-context Enter after global precedence: invalid data consumed with focus moving to the typed first-invalid field and the reason rendered verbatim inline — no validation/estimation/execution/history commands; runnable data emitting only the `PreExecutionRequestedMsg` seam for Issue #21; opener fields preserving Issue #12–#18 behavior with the authoritative Limit exception showing exactly the invalid-Limit reason; transient reasons cleared by the next `applyBuilder` rebuild), `internal/ui/model.go` (base Where Backspace/Delete routed through `ClearWhereValue`), and `internal/ui/command_table.go` (Set/Insert field-bar entries representing write prompt targets). Tests: `internal/querybuilder/runnable_test.go`, `whole_value_test.go`, `internal/ui/whole_value_clearing_test.go`, `runnable_feedback_test.go`. Cross-referenced Issues #15–#19 with forward references to Issues #21, #37, #38, and #39 plus the Runnable-State Contract, Builder and Display Interaction, Global Key Precedence, QueryBuilder, UI, and Testing Decisions of `Notes/PRD-sqloid.md`. Created [runnable-state-feedback.md](runnable-state-feedback.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.

## [2026-08-27] ingest | Issue #20 minimal query-history append (stable ID + consecutive suppression)

Ingested the Issue #20 implementation: `internal/querybuilder/history_state.go` (canonical `HistoryState` snapshot via `QueryBuilder.HistoryState()` — command, stable table identity, ordered projection, WHERE presence/column/operator/entered value/parsed bound type, GROUP BY acceptance order, ORDER BY expression/direction, Limit empty-vs-number, ordered UPDATE/INSERT choices with values and exact entered representations, all slices freshly copied, transient focus/draft/cursor/error/layout/request state excluded — plus normalized `Equal` proving entered representation, bound type, choice, and every ordering significant), `internal/history/query_store.go` (session-only store with nonzero monotonically stable `EntryID`s independent of positions, immutable deep-copied states on append and retrieval, chronological order, exact `Capacity = 20` with oldest-first eviction preserving surviving IDs, deterministic empty/not-found behavior, no database/Bubble Tea dependency), `internal/history/query_append.go` (the single `AppendExecution` entry point suppressing only normalized-equal immediately preceding executions without consuming an ID or evicting, retaining A→B→A), and the `internal/ui/model.go` timing seam (`History *history.Store` field, `ExecutionStartedMsg` → `appendQueryHistoryAtExecutionStart` appending only for SELECT/INSERT, `PreExecutionRequestedMsg` explicitly appending nothing, UPDATE/DELETE awaiting Issues #37/#38 confirmation, Issue #22 to emit the start message after successful validation so failed executions keep their start append). Navigation, cursors, restoration, result history, and selected-entry eviction fallback remain deferred to Issue #35. Tests: `internal/history/query_store_test.go`, `internal/history/query_append_test.go`, `internal/querybuilder/history_state_test.go`, and narrow `internal/ui/query_history_append_test.go`. Cross-referenced Issues #19, #20, #22, and #35 and the Execution and Result Lifecycle, History implementation decision, QueryBuilder, UI, and Testing Decisions of `Notes/PRD-sqloid.md`. Created [query-history-append.md](query-history-append.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #21 pre-execution schema-version validation

Ingested the completed Issue #21 implementation (Tasks 1–4): `internal/schema/revalidate.go` (typed `RevalidateStatus` unchanged/refreshed/refresh-failed/deleted/replaced outcomes with `Revalidate(prior, version, refresh)` and the `VersionAttempt` transport type), `internal/querybuilder/revalidate.go` (immutable repair transition clearing only state transitively dependent on an invalidated object, identifier, insertability fact, or rowid property, preserving unrelated Limit/entries, returning the authoritative post-repair `RunnableReport`), `internal/connection/schema.go` (`ReadSchemaVersion` as one cancellable request with boundary identity checks), and `internal/ui/schema_validation.go` (runnable Enter→`PreExecutionRequestedMsg`→`beginValidation` under fresh preparation identities; unchanged-version cache reuse without a catalog refresh; changed-version `QueryBuilder.Revalidate` repair with first-specific-reason focus and no execution when non-runnable; ordinary refresh failure retaining stale data behind retry with fresh identities and Esc cancel restoring the exact builder context; Ctrl+W dispatching connection-scoped cancellation exactly once with exact `cancelling…` until settlement and cancellation-wins late-success discard; deletion/replacement terminal precedence rejecting late completions; execution-start route (`ExecutionStartedMsg`, replaced by Issue #22) returned only on settled success; validation appends neither query nor result history). Documented unchanged reuse, refresh/repair, cancellation, no-history behavior, health precedence, and the post-validation-DDL-as-ordinary-execution-error distinction. Created [schema-validation-workflow.md](schema-validation-workflow.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.

## [2026-08-27] ingest | Issue #22 first end-to-end SELECT and result grid

Ingested the completed Issue #22 implementation (Tasks 1–7): `internal/result` (typed `Value`/`Kind` NULL/INTEGER/REAL/TEXT/BLOB never collapsed; full-set left-to-right collision-safe `DeduplicateNames` with original driver labels kept separately from deduplicated `HeaderNames()`; `RealToken` exact shortest round-trip finite-REAL token with `.0` restoration, locale-independent; `GridText` visible `⇥`/`⏎` control-character symbols; `DecodeText` maximal invalid-UTF-8 replacement to exactly one U+FFFD per maximal subpart with `InvalidUTF` warning metadata; exact byte-for-byte BLOB retention with `[BLOB n bytes]` display; `FromDriver` single boundary conversion), `internal/connection/firstpage.go` (`RunFirstPage` as one complete `RunRequest` on a dedicated lease with eager typed scanning and Issue #6/#7 behavior preserved), the `internal/ui` orchestration (`SelectExecutor` seam, `ExecutionStartedMsg` appending history then issuing exactly one first-page request at the actual-execution boundary, `SelectSettledMsg` settling into `Result *ResultView`, no driver type in Bubble Tea state), and the frozen-header grid (`renderResultPage`: deduplicated header, absolute inclusive `rows 1-N` range, typed cells, persistent invalid-UTF warning, exact `No rows`, ordinary execution-error boundary, capped-width ellipsis). Documented the exact query-history append boundary (after successful validation, before any database work), the full-set output-name collision rule, and the architectural requirement that the grid and future CSV/JSON exporters share `internal/result` rather than duplicate representation logic (enforced by `internal/result/architecture_test.go` parsed-source assertions). Recorded the complete removal of the disposable Issue #10 tracer runtime path (`internal/ui/tracer.go`, `internal/connection/tracer.go`, `internal/schema/tracer.go`, `Model.Trace`, all tracer messages and fixtures) with retained reusable test fixtures/helpers (fake refresher/executor controls, SQLite integration harnesses, shell/layout assertions). Cross-referenced Issues #10, #21, and #22 and the Builder and Display Interaction, Execution and Result Lifecycle, Grid rendering/cache, Invalid UTF-8 TEXT, Output names, Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [first-select-result-grid.md](first-select-result-grid.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.

## [2026-08-27] ingest | Issue #23 non-finite REAL grid rendering

Ingested the completed Issue #23 implementation (Tasks 1–4): `internal/result/result.go` extends `RealToken` with the exact grid tokens for non-finite REALs — `Inf` for positive infinity, `-Inf` for negative infinity, and exactly `NaN` for every NaN bit pattern (quiet, negative, arbitrary payloads; payload bits never inspected or exposed) — selected only after the value is known to be `KindReal`, so finite REALs keep the Issue #22 shortest-round-trip token with `.0` restoration and non-finite values never reach finite formatting or invalid-UTF handling. Rendering stays display-only: rows, snapshots, and metadata retain the float64-backed REAL value including exact NaN bits, TEXT values containing the literal strings `Inf`/`-Inf`/`NaN` keep `KindText` and render verbatim through `GridText` despite identical glyphs, and no warning metadata or normalization is added. `internal/ui`'s grid continues consuming the shared seam with no private branch. Tests: `internal/result/result_test.go` (`TestNonFiniteRealDisplayTokens`, `TestNonFiniteRealTypeAndValueRetained` — direct construction plus `FromDriver` scans with exact float64-bit and kind retention, no invalid-UTF metadata, unchanged row/column counts) and `internal/ui/results_grid_test.go` (`TestResultGridNonFiniteRealTokens` — frozen-grid rendering with exact per-row tokens via `gridCellTexts`, finite tokens unchanged, backing values retained). Documented the grid-only nature of the policy: JSON's later quoted-string form and CSV's later textual form for non-finite REALs are separate exporter policies drawing from the same typed representation. Cross-referenced Issues #22 and #23 and the Numeric value parsing and rendering, Grid rendering/cache, Export formats and values, Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [non-finite-real-grid.md](non-finite-real-grid.md); updated `first-select-result-grid.md`, `source-code.md`, `unit-tests.md`, and `index.md`.

## [2026-08-27] ingest | Issue #24 concurrent first page and independent result count

Ingested the completed Issue #24 implementation (Tasks 1–6): `internal/querybuilder/count_sql.go` (`CountSQL()` rendering exactly `SELECT COUNT(*) FROM (<SelectSQL()>)` — the complete SELECT counted as a subquery with the user's Limit inside it, so rows beyond the Limit are irrelevant and the wording never implies a table or pre-Limit count — plus `CountParams()` returning exactly `SelectParams()` in unchanged order), `internal/result/count.go` (explicit `CountStatus`/`CountState` lifecycle with `Header()` rendering exactly `Counting rows…`, `Result count: N`, `Result count: N (after Limit M)`, or `Count unavailable`; HasLimit/Limit as executed metadata; zero value renders nothing so callers render only from explicit state, never from row length) and `internal/result/select_identity.go` (monotonic nonzero `NextSelectExecutionID`/`NextSelectRequestID`, distinct `RoleFirstPage`/`RoleCount` with no interchangeable identity, and `SelectTracker` enforcing the two-level identity rule with at-most-once role consumption so delayed, duplicated, cancelled, or superseded completions mutate nothing), `internal/connection/count.go` (`DB.RunCount` as one complete `RunRequest` on its own dedicated lease scanning the wrapped complete-SELECT COUNT(*) once as int64, typed `countError` wrapping, Issue #6/#7 classifications preserved, never reinterpreted as a page failure) plus documented test-only barrier seams (`DB.beforeFirstPage`/`DB.beforeCount`) and the mandatory WAL/rollback-journal capability suite `count_overlap_test.go` (simultaneous barrier-held overlap, distinct physical connections, third-lease blocking, unchanged journal mode, independent-snapshot drift across an interleaved external writer, rollback-journal delay or ordinary `database is locked` error, count-failure isolation, clean lease release, no sleeps), and the `internal/ui` orchestration (`startSelectPage` assigning one execution ID plus two distinct role request IDs, installing the tracker, capturing the executed Limit as count metadata, and launching both requests via `tea.Batch` without waiting for either; `SelectSettledMsg`/`CountSettledMsg` carrying both identity levels with Update gating every completion through the tracker; `count.go`'s `CountExecutor`/`CountResult` seam, `applyCountSettled` explicit count state, and `CountHelpLines`; the status/count line composing the exact count wording with the independently displayed range, rows never clamped to an inconsistent count, count failure rendering exactly `Count unavailable` with retained rows and continued paging, and first-page failure following its ordinary result-error boundary independently of count). Issue #26 later-page identities and viewport generations deliberately absent. Tests: `count_sql_test.go`, `count_test.go`, `select_identity_test.go`, `count_overlap_test.go`, `concurrent_count_test.go`. Cross-referenced Issues #5, #6, #22, and #24 and the Paging consistency, Connection pool, Execution and Result Lifecycle, Module Design, and high-risk Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [concurrent-page-count.md](concurrent-page-count.md); updated `source-code.md`, `unit-tests.md`, `first-select-result-grid.md`, `connection-pool.md`, and `index.md`.

## [2026-08-27] ingest | Issue #25 serialized vertical result paging

Ingested the completed Issue #25 implementation (Tasks 1–6): `internal/querybuilder/page_sql.go` (`PageSQL(pageLimit, offset)` rendering the base SELECT with any user ORDER BY kept byte-for-byte, the single eligible implicit `ORDER BY rowid` fallback, and an exact integer `LIMIT <limit> OFFSET <offset>` range whose limit is clamped to the remaining user Limit so a page never reads beyond it, with beyond-Limit/invalid/unrenderable cases yielding empty and `PageParams()` exactly `SelectParams()`; the fallback applies only to an ordinary rowid table with no declared `rowid`/`_rowid_`/`oid` shadow, no user ORDER BY, and no aggregate/group shape — views, virtual tables, WITHOUT ROWID tables, all declared-shadow spellings, aggregate-only and grouped queries, ties, and concurrent writes carry no stability claim, while explicit aggregate/grouped ASC/DESC ordering is preserved without an appended tie-breaker), `internal/connection/page.go` (`DB.ExecutePage` running exactly one bound page statement as a complete `RunRequest` on its own dedicated lease with first-page scanning/conversion rules and preserved health classification), and the `internal/ui` orchestration (`paging.go`: `PageExecutor`/`PageSettledMsg` seam, `handlePageKey` issuing exactly the adjacent absolute logical range at the layout-derived complete-visible-row size from `CalculateLayout().PageRows`, serialized one-pending-page state independent from Issue #24's count request, consumed repeated/opposite keys with exact `loading next page…` status feedback while horizontal movement stays local, `applyPageSettled` installing matched completions with absolute `rows <offset+1>-<offset+N>` status and marking the high boundary on short pages, `resetPagingState` on fresh executions; `results_grid.go`/`first_select.go`: absolute-range status and the loading indicator composed into the status/count line without clamping or replacing displayed rows). Tests: `internal/querybuilder/page_sql_test.go` (table-driven page SQL, ordering policy, exclusions, clamping), `internal/ui/vertical_paging_test.go` (scripted adjacency, boundaries, held-pending suppression with request counts, count-plus-one-page overlap, exact sizes 11/15/34 at 80×24/100×30/160×50 plus resize), and `internal/connection/page_test.go` (adjacent disjoint ranges, typed empty page, ordinary failure). Issue #26 later-page identities/viewport generations and Issue #28 cancellation remain deliberately absent. Cross-referenced Issues #22, #24, and #25 and the Paging consistency, Grid rendering/cache, Resize/layout, Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [serialized-vertical-paging.md](serialized-vertical-paging.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-28] ingest | Issue #26 SELECT request identities and stale-response rejection

Ingested the completed Issue #26 implementation (Tasks 1–4): `internal/ui` (`Model.viewportGen` with `bumpViewportGeneration`; `deactivateActiveSelect` as the finalization seam called by `startSelectPage` so a new execution finalizes the previous SELECT before dispatching; `resize` advancing the generation on every visible resize and on restore-from-suspension while suspension entry stays exactly frozen; `SelectSettledMsg` gaining Generation and `PageSettledMsg` gaining ExecutionID/Generation with commands capturing immutable identities at dispatch; centralized acceptance in Update — tracker + current generation + not-cancelled for first pages, and in `applyPageSettled` request-ID-matched settlement with execution/generation/cancellation gating for rows, range, and exhausted-boundary mutation so mismatched responses can never clear a newer request's pending guard; `FirstPageResult`/`CountResult` gaining the boundary's `Cancelled` classification with cancelled counts and first pages fully inert), `internal/result/select_identity.go` (`SelectTracker.ExecutionID()` accessor for page dispatch), and `internal/connection` unchanged in production code (lease ownership already prohibits reuse before settlement; `Request.Settle` already classifies post-cancellation success as `OutcomeCancelled`). Tests: `identity_guards_test.go` (message-deferred barriers, out-of-order first/later page successes and failures across execution IDs, request IDs, and generations; resize/deactivation/superseded-execution rejection; replacement after resize within the same execution; stale responses unable to clear newer feedback or cache metadata; current control case), `cancellation_settlement_test.go` in both `internal/ui` (cancellation wins over late success on all roles; inert cancelled/late-error responses after a newer execution; replacement refusal before predecessor settlement; count-first independent settlement while a page is pending; newer-execution dispatch through the execution-start seam) and `internal/connection` (barrier-proven lease holding through the cancelling state until settlement with `OutcomeCancelled` classification and clean reuse, independent page/count settlement in either order), plus suspension-snapshot exclusion of the viewport generation and real-execution fixtures replacing zero-identity literals in the view tests. Cross-referenced Issues #6, #8, #24, #25, #26, and #28 and the Identities and state, SELECT, Errors and cancellation bounds, Global Key Precedence, Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [select-request-identities.md](select-request-identities.md); updated `concurrent-page-count.md`, `serialized-vertical-paging.md`, and `index.md`.
## [2026-08-28] ingest | Issue #27 in-flight feedback and execution gating

Ingested the completed Issue #27 implementation (Tasks 1–6): `internal/ui/inflight_gate.go` (the generic request-in-flight gate deriving pending state solely from request-ownership flags — `firstPagePending`, Issue #25's `pagePending`, and the new `countPendingFlag` — never from rendered phase-label strings; blocked Enter/history/save/export consumed with exact explanatory feedback (`query history is unavailable while a request is in flight`, `result history is unavailable…`, `saving is unavailable…`, `export is unavailable…`) and no command dispatch so request counts never change; phase-specific Enter feedback with the exact `press Ctrl+W to cancel` hint; permitted local horizontal `,`/`.` movement, serialized page keys, field navigation, and base help falling through ungated; `q`/Ctrl+C opening the shared quit confirmation; Ctrl+W routed only to an owned cancellable request, dispatching exactly once), the shared quit confirmation (`quitConfirm`/`quitSuspended` snapshot above every context; Enter/y/Ctrl+C confirming after `deactivateActiveSelect` cleanup with `tea.Quit`; Esc/n restoring the exact suspended context with no key leakage), scoped Ctrl+W cancellation handoff (`selectCancelling` marked before dispatch, `SelectCancelRequestedMsg` inert at Update, exact `cancelling…` until every owned request settles — interrupt semantics untouched for Issue #28), and read-phase feedback (`Running…` for first-page work via `SelectRunningIndicator`, `Counting rows…`/`Count unavailable` from the Issue #24 count state, distinct `loading next page…` from Issue #25, status/count line composition via `joinStatusParts(count, running, cancelling, loading, range)`, footer rendering `inFlightNotice` transiently). Claim release at settlement regardless of outcome classification (success/failure/cancellation) keeps the gate unwedgeable under Issue #26's stale-response guards. Issue #44 owns write-phase labels and write-side gate integration; no write-phase label appears on any read path. Tests: `internal/ui/inflight_gating_test.go` (table-driven gating across first-page/later-page/count phases, precedence covering terminal/quit-confirmation/top-overlay/focused-input/pending/base, flag-driven gating without labels, settlement release in either order) and `internal/ui/read_feedback_test.go` (exact label contracts, either-order settlement, feedback surviving local interaction, action-specific rejections, no-write-phase-label guard). Cross-referenced Issues #6, #8, #21, #24, #25, #26, #28, #29, #44, and #55 and the Global Key Precedence and Context/Action Matrix, Execution and Result Lifecycle, Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [in-flight-gating.md](in-flight-gating.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-28] ingest | Issue #28 scoped Ctrl+W cancellation and bounded settlement

Ingested the completed Issue #28 implementation (Tasks 1–5): `internal/connection/started_request.go` (StartedRequest/StartedPageRequest/StartedCountRequest handles wrapping Issue #6's Request lifecycle on dedicated leased connections with idempotent connection-scoped Cancel, lease release only at true settlement, Wait/SettledChan all-request settlement visibility, pre-settled lease failures, and QueryBuilder-owned SQL), `internal/ui` scoped Ctrl+W wiring (per-request derived cancellation contexts for first page, later page, and count installed at dispatch and retired at settlement; SelectCancelRequestedMsg cancelling exactly the active set; generic cancellation seam re-armed by later-page dispatch; `cancelling…` until the last settlement; replacement dispatch deferred; Issue #26 identity guards keeping late cancelled results inert), and the mandatory Linux/macOS capability suite `internal/connection/select_cancellation_capability_test.go` (probe-bearing virtual generated columns with drained barrier signals proving querybuilder-generated page/count work verifiably started; independent page/count and later-page interrupts settling within one second; no lease reuse while cancelled requests are unsettled; lock-wait settlement within the five-second busy-timeout window with the documented pinned-driver reality that sqlite3_interrupt does not preempt busy waits — SQLITE_BUSY at expiry classifies as failed with the cause preserved; cancellation-wins late-success discard; healthy subsequent work with no Sqloid force-close). Recorded the ownership split: schema validation → Issue #21, estimate → Issue #41, beginning/executing writes → Issue #42, post-commit boundary → Issue #43. Tests: `internal/connection/select_cancellation_test.go`, `select_cancellation_capability_test.go`, `internal/ui/select_cancellation_test.go`, plus the `settleFirstPage` helper now retaining the count settlement model. Cross-referenced Issues #6, #21, #24–#28, and #41–#43 and the Identities and state, SELECT lifecycle, Global Key Precedence, Errors and cancellation bounds, Connection/UI Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [scoped-select-cancellation.md](scoped-select-cancellation.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.

## [2026-08-28] ingest | Issue #29 whole-column horizontal scrolling

Ingested the completed Issue #29 implementation (Tasks 1–6): `internal/ui/horizontal_layout.go` (the pure whole-column seam — `visibleGridLayout(names, cells, availWidth, first)` deriving the visible columns and display widths afresh from the deduplicated output columns, rendered cell texts, available grid width, and the first-visible output-column index, packing the first column always with an oversized first column capped to the available cell area and later columns only when they fit completely with the `" | "` separator, so "no room for another complete column" excludes rather than clips; `horizontalStep` whole-column stepping with first/last/empty/single-column boundary no-ops; `clampFirstColumn` preserve-then-clamp normalization; `naturalGridWidths` shared Unicode display-width arithmetic capped at `gridColumnCap`), `internal/ui/horizontal_keys.go` (`Model.firstColumn` as the grid's only horizontal position with no character or byte offset anywhere; `.`/`,` base bindings and the xterm Shift+Page CSI bridge `shiftPageDirection`/`ShiftPageMsg` since Bubble Tea reports `ESC[6;2~`/`ESC[5;2~` as unknown CSI messages; `handleHorizontalKey` moving exactly one whole accepted column, issuing no database command, and staying local while any SELECT page or count request is pending; `clampFirstColumnModel`), `internal/ui/model.go` (`firstColumn` field, base-context key cases, Update CSI bridge, resize and suspension-restore clamping), `internal/ui/paging.go` (`resetPagingState` resetting the index to zero on fresh executions), and `internal/ui/results_grid.go`/`view.go` (`renderResultPage` rendering only the laid-out whole columns of the frozen deduplicated header and rows at the results interior width, complete-row vertical allocation and status/count composition unchanged). Tests: `internal/ui/horizontal_layout_test.go` (table-driven wide/narrow/Unicode/exact-fit/no-room/oversized-cap/both-end clamping/empty/idempotence, boundary stepping, resize clamping) and `internal/ui/horizontal_keys_test.go` (scripted exact-one-column moves for all four bindings, both boundary no-ops, no-command/request-count invariance, held first-page/later-page/count locality through the fake executor seams, raw-CSI bridge, quit/popup/focused-input/too-small/terminal precedence, narrow-viewport render hiding the abandoned first column and revealing the next, resize preservation and clamping). Cross-referenced Issue #29 and the UI Behavior, SELECT lifecycle, Global Key Precedence and Context/Action Matrix, Keybinding portability, Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [whole-column-horizontal-scrolling.md](whole-column-horizontal-scrolling.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.

## [2026-08-28] ingest | Issue #30 contiguous 10,000-position result cache

Ingested the completed Issue #30 implementation (Tasks 1–4): `internal/resultcache/cache.go` (the UI-independent positional cache — `Position` as absolute one-based logical row identity keeping duplicate-valued rows distinct, `Page`/`Row` fetch units over `result.Value` payloads copied at acceptance, `Cache.Merge(page, dir)` accepting any nonempty page into an empty cache and otherwise exactly when the page/cached union stays contiguous after accounting for overlap, a two-way sorted merge that preserves ascending absolute-position order for backward prepends and forward appends while replacing same-position rows with the page payload without duplication, and atomic rejection of stale pages that would create low-side or high-side gaps with rows, metadata, and counters untouched), the independent `MaxPositions = 10000` hard cap (deterministic low-end eviction for accepted forward merges and high-end eviction for accepted backward merges by exactly the excess count, no eviction at exactly the cap, first/last 10,000 retention for oversized pages, cumulative `RowCapEvictions` metadata, `Start()`/`End()`/`Len()`/`Rows()` range metadata exposed for `internal/ui` and later immutable snapshots, zero-value caches rejecting merges, and the 64 MiB byte cap deliberately deferred to Issue #31). Tests: `internal/resultcache/cache_test.go` (table-driven positional identity: initial insertion, duplicate values at distinct positions, forward append, backward prepend, exact/partial overlap replacement at each edge, whole-range and single-edge-spanning pages, repeated overlap, alternating traversal, low/high gap rejection with unchanged state, empty-page and zero-value rejection) and `internal/resultcache/cap_test.go` (forward/backward traversal beyond 10,000 positions in exact-cap, one-past, multi-page, and oversized-page shapes; overlap replacement near each edge and spanning an edge under cap pressure with retained overlap values proven; alternating direction after prior eviction; stale gap pages after eviction rejected atomically; shared per-merge assertions of exact range metadata, one ascending gap-free row per position within the cap, and no duplicates). Cross-referenced Issues #24, #25, #26, #29, #30, and #31 and the Cache and snapshot invariant, History and UI Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [positional-result-cache.md](positional-result-cache.md); updated `index.md`.

## [2026-08-29] ingest | Issue #31 64 MiB cache cap and oversized-value handling

Ingested the completed Issue #31 implementation (Tasks 1–6): `internal/resultcache/payload.go` (pure exact retained-payload accounting — raw TEXT/BLOB byte length, exactly 8 bytes for each INTEGER/REAL, 0 for NULL, no display width or formatted token length, no Go/model container overhead, byte-for-byte BLOB identity preserved), `internal/resultcache/cache.go` (`MaxPayloadBytes = 64 << 20` enforced cumulatively and independently with `MaxPositions` after every accepted merge, complete-row opposite-end byte eviction, `PayloadBytes()` exact totals maintained through merges/replacements/evictions, sticky `TruncatedByByteCap()` disclosure surviving later navigation below the cap, `Merge(page, dir)` page-envelope admission retaining only the complete rows nearest the retained range in traversal order — leading for forward, trailing for backward — with typed `*result.LimitFailure{KindPage}` naming the first nonfitting one-based absolute logical position, no bytes or fields of the failing row retained, prior cache rows preserved, and contiguity restored under both caps), `internal/result/limits.go` (the authoritative shared definitions `ByteCapWarning = "Result truncated: 64 MiB cache limit"`, `LimitKind` KindPage/KindValue, and `LimitFailure.Error` producing the exact `result page exceeds the 64 MiB v1 limit at row N` / `result value exceeds the 64 MiB v1 limit at row N` messages), `internal/connection/firstpage.go` (`ExecutePage` gaining the page OFFSET so value-limit failures report one-based absolute logical positions; typed `SQLITE_TOOBIG` classification via the driver error code at execute/scan/iterate plus Sqloid's raw-value size check; the oversized row never exposed, earlier complete rows returned with exact BLOB bytes), and `internal/ui` (`ResultView`/`FirstPageResult` carrying `ByteTruncated` and `LimitFailure` metadata, `applyPageSettled` inheriting the disclosure across traversal, `renderResultContent` composing the shared warning and typed failure messages into the status line without any UI-private literal). Tests: `internal/resultcache/payload_test.go`, `bytecap_test.go` (exact-boundary/one-byte cases both directions, opposite-end complete-row eviction, cap coexistence, persistent disclosure, atomic stale rejection after eviction, row-cap-only eviction not setting byte metadata), `admission_test.go` (non-first forward/backward page-envelope trims at exact positions with prior rows preserved and contiguous ranges, boundary pages admitted fully, TEXT/BLOB equivalence, provably distinct failure kinds), `internal/connection/value_limit_test.go` (integration through modernc.org/sqlite: 64 MiB + 1 byte BLOB failing typed at the absolute row with exact message and byte-exact earlier rows, boundary-sized value succeeding, non-first-page positions via `ExecutePage`), and `internal/ui/bytecap_test.go` (shared warning rendered exactly once from `result.ByteCapWarning`, absent for row-cap-only results, persistent through traversal and failed settlements, single-literal architecture assertions, exact typed failure messages). Cross-referenced Issues #5, #30, and #31 and the Cache and snapshot invariant, Errors and cancellation bounds, Connection/UI/History Module Design, and high-risk Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [byte-cap-oversized-values.md](byte-cap-oversized-values.md); updated `index.md` and the Issue #30 staleness in [positional-result-cache.md](positional-result-cache.md).

## [2026-08-29] ingest | Issue #32 resize-safe vertical viewport recovery

Ingested the completed Issue #32 implementation (Tasks 1–5): `internal/ui/viewport_recovery.go` (the pure, UI-independent recovery seam — `RecoveryAction` preserve/clamp-low/clamp-high/fetch, `ViewportMeta` carrying the retained range, dual-cap eviction disclosure, exhausted-final-page flag, and settled count, and `RecoverViewport(meta, prior, newSize)` applying the decision order: preserve the exact prior first logical row while valid and retained, clamp to the retained low endpoint below the range, clamp to the retained high endpoint only when established by a short final page or a count not exceeding the retained end so an inconsistent count never clamps, else fetch the absolute containing page `((prior-1)/newSize)·newSize+1` at the exact new size floored at one row, with empty/unknown metadata deterministically fetching), `internal/ui/resize_recovery.go` (orchestration — lazy per-SELECT cache construction, `applyResizeRecovery` recomputing `CalculateLayout(m.Height, m.Fields).PageRows` from complete visible rows and running the seam, local `rebuildRetainedView` re-slicing retained rows with high-clamp marking the exhausted boundary, `requestRecoveryPage` dispatching exactly one cancellable `LIMIT <newSize> OFFSET <containingStart-1>` request through the existing identity/generation/cancellation lifecycle, and `mergePageIntoCache` merging accepted pages by absolute position with traversal-direction eviction), `internal/ui/paging.go` (fresh-execution `resetPagingState` replacing the cache so a new first page never merges into a previous result's stale retained range, and accepted-page settlement merging into the cache before display), `internal/ui/model.go` (`viewportCache` state, the `resizeFetchPending`/`resizeFetchRow`/`resizeFetchSize` deferral overwritten by repeated resizes, `WindowSizeMsg` relaying into recovery after generation-advancing resize, the PageSettledMsg settlement gate dispatching exactly one deferred replacement only after true settlement, and first-page cache seeding in `first_select.go`), and `internal/connection` reusing the existing cancellable page-request lifecycle unchanged. Tests: `internal/ui/viewport_recovery_test.go` (pure table-driven preservation, single-row ranges, both endpoint clamps, count-established endpoints with inconsistent-count exclusion, empty-cache determinism, row-cap/byte-cap eviction equivalence, absolute containing-page boundary arithmetic, non-positive page sizes) and `internal/ui/resize_recovery_model_test.go` (scripted model coverage with deterministic fakes: idle preserve/clamp/fetch with exact SQL, pending old-size generation advancement with late success and late failure rejection and no row overwrite, mandatory settlement before exactly one correctly sized containing-page replacement, repeated resize coalescing to the latest size, count independence, count-only and page-plus-count concurrency, too-small suspension with restoring-resize recovery, inactive/finalized no-fetch controls, grow/shrink/unchanged page-size recomputation, fresh-execution cache replacement, and shared contiguity/10,000-position/64 MiB invariant assertions after every accepted response). Documented the decision order, recomputation, generations, settlement, and branch rules in [resize-vertical-recovery.md](resize-vertical-recovery.md). Cross-referenced Issues #25, #26, #30, #31, and #32 and the SELECT lifecycle, Cache and snapshot invariant, UI/Connection Module Design, resize Testing Decisions, and manual layout matrix sections of `Notes/PRD-sqloid.md`; updated `index.md`.

## [2026-08-29] ingest | Issue #33 snapshot metadata, completeness, and endpoints

Ingested the completed Issue #33 implementation (Tasks 1–6): `internal/history/snapshot.go` (immutable typed `SnapshotMetadata` — optional inclusive retained range, optional known total of the limited logical result, reached-low/high endpoint observations, cumulative row-cap eviction and persistent `truncated-by-byte-cap` facts, invalid-UTF status, independently typed terminal outcome with reason and optional one-based failure position; shape validation without rewriting observed facts; pure value semantics so finalized metadata never changes; `CacheFacts`/`FactsFromCache` as the narrow conversion boundary from the authoritative resultcache), `internal/history/snapshot_classify.go` (exclusive `complete` requiring both endpoints established and full limited-result retention with no eviction, finished work, and consistent evidence; coexisting truthful `partial`/`truncated`; high endpoint from a known total or an actually observed short/empty final page only — never an unobserved remainder; no clamping of contradictory count/cache evidence; terminal outcomes orthogonal), and `internal/ui/snapshot_metadata.go` (`Model.SnapshotFacts` converting settled count state, viewport-cache facts, and paging/cancellation seam inputs into immutable metadata plus traversal facts independent of the model). Tests: `internal/history/snapshot_test.go` (metadata matrix, validation, value-semantics mutation attempts, no-presentation-duplication source scan), `internal/history/snapshot_classify_test.go` (complete/partial/truncated matrix, limited-result semantics, count failure, short/empty observation, unknown remainder, zero retained rows, no clamping, terminal-outcome independence), `internal/resultcache/snapshot_boundary_test.go` (facts track the cache lifecycle, ascending positions after backward traversal, persistent disclosure below the cap), and `internal/ui/snapshot_finalize_test.go` (model finalization cases and later-navigation independence). Documented every field, label combination, endpoint rule, and the separation from the shared Issue #31 warning in [snapshot-metadata.md](snapshot-metadata.md). Cross-referenced Issues #24, #30, #31, and #33 and the SELECT lifecycle, Cache and snapshot invariant, UI/History Module Design, and metadata Testing Decisions sections of `Notes/PRD-sqloid.md`; updated `index.md`.

## [2026-08-29] ingest | Issue #34 active-SELECT lifetime and single finalization

Ingested the completed Issue #34 implementation (Tasks 1–4): `internal/ui/active_select.go` (the explicit active-SELECT lifecycle seam — `SelectIsActive`/`ActiveSelectExecutionID`/`FinalizedSelectExecutionID` over `selectActive`/`activeExecID`/`finalizedExecID` state kept separate from request identity, `activateSelect` finalizing the previous active SELECT before a new execution replaces the lifetime, `enterResultHistory` as the result-history finalizer seam, `acceptedQuitCleanup` for accepted quit with required cancellation/settlement, `finalizeActiveSelect` marking the execution finalized before any duplicate or late message can append or rewrite, and `appendFinalizedResultEntry` building the exactly-once immutable snapshot — tabular success/count-failed/partial-failed/cancelled-after-rows and failure-after-rows entries carrying ascending retained rows, columns, Issue #33 `SnapshotMetadata`, and truthful `Classify` completeness, plus non-tabular `KindCancelled`/`KindError` entries when cancellation or first-page failure occurs before rows), `internal/ui/model.go` (the `ResultHistory *history.ResultStore` field, first-page cancelled/failure finalization after settlement, later-page recorded endings cleared by a healthy page settlement, and page recovery refusal once inactive), `internal/ui/paging.go` and `internal/ui/resize_recovery.go` (page dispatch and recovery fetches refused after finalization, resize generation advancement preserved as non-finalizing), `internal/ui/inflight_gate.go` (accepted quit routed through `acceptedQuitCleanup`), `internal/ui/first_select.go` (actual-new-execution finalization before replacement), and `internal/history/result_entry.go` (`ResultEntry` with `ResultKind` tabular/Cancelled/error and immutable deep-copied rows plus `ResultStore.AppendFinalized` deterministically rejecting a second entry per execution ID). Tests: `internal/ui/active_select_lifecycle_test.go` (the table-driven lifecycle matrix over count-pending/page-pending/settled combinations: builder edits, overlays/help, save/export, query-history keys, resize, serialized paging, page/count settlement, count failure, validation opening, and unaccepted quit all preserve activity; each finalizer ends it exactly once; late messages cannot reactivate a finalized SELECT), `internal/ui/snapshot_finalize_once_test.go` (exactly-once snapshot outcomes, metadata carried from the authoritative cache and immutable against later cache activity, duplicate/late/repeated finalizers harmless, quit with pending work), and `internal/history/result_entry_test.go` (append-once idempotence, retained-entry immutability against caller and returned-copy mutation, non-tabular entry shapes, `FactsFromCache` snapshot independence). Documented the active/request/execution/generation distinctions, the exhaustive finalizing and non-finalizing lists, and the snapshot outcomes in [active-select-lifetime.md](active-select-lifetime.md). Cross-referenced Issues #26, #33, and #34 and the Identities and state, SELECT, Cache and snapshot invariant, Global Key Precedence and Context/Action Matrix, UI/History Module Design, and active-finalization Testing Decisions sections of `Notes/PRD-sqloid.md`; updated `index.md`.
