# Wiki Log

## [2026-08-26] ingest | Issue #1 CLI shell and usage contracts

Initial wiki setup and ingest of the Issue #1 implementation: created `project-overview.md`, `source-code.md`, `cli-contract.md`, and `unit-tests.md`; documented the `mow.cli` command structure, the `sqlite <file>` and `d1` commands, help/version flags, exact stream ownership, exit statuses (0/2), silent successful startup, and the `internal/cli` vs `cmd/sqloid` roles. Updated `index.md`.
## [2026-08-26] ingest | Issue #2 SQLite startup validation and read-write errors

Ingested the Issue #2 implementation: pinned `modernc.org/sqlite v1.57.0`; created `internal/connection` (`Open`, `Session`, structured `StartupError` classes) with ordered non-mutating pre-open validation (existence → readability → exact header → writability proof → mode=rw open → schema probe); connected the sqlite handler in `internal/cli` to render exact one-line diagnostics with status 1 while `cmd/sqloid` stayed a thin entrypoint. Added [sqlite-startup.md](sqlite-startup.md); updated `cli-contract.md`, `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-26] ingest | Issue #3 local D1 candidate discovery

Ingested the Issue #3 implementation: created `internal/d1` (`Discover`, exact-rule `eligible` filter, typed `ErrNoCandidate`/`ErrMultipleCandidates` sentinels) scanning only `.wrangler/state/v3/d1/miniflare-D1DatabaseObject` non-recursively; wired `internal/cli.RunD1` to pass the sole candidate unchanged to `internal/connection.Session` with no D1-specific opening path; fixed `mustFileURL` so relative paths render as opaque `file:` URIs (no invented URI authority). Created [d1-discovery.md](d1-discovery.md); updated `cli-contract.md`, `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-26] ingest | Issue #4 exact D1 discovery diagnostics

Ingested the Issue #4 implementation and golden tests: `internal/cli.mapDiscoveryDiagnostic` now maps typed `internal/d1` outcomes to exact process diagnostics — zero candidates (missing, unreadable, empty, or candidate-free Wrangler directory) emit exactly two stderr lines (`no candidate database found in .wrangler` plus the expected-path and `sqloid sqlite <file>` explicit-open hint), multiple candidates emit only `There is more than one SQLite database in .wrangler` with no hint; both bypass the opener, exit 1, and create nothing. Added discovery-failure section to [d1-discovery.md](d1-discovery.md); updated `cli-contract.md`, `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-26] ingest | Issue #5 two-connection SQLite pool and limits

Ingested the Issue #5 implementation: `internal/connection` now opens an exact-two pool (minimum and maximum both 2), applies the five-second busy timeout to every physical connection via the DSN `_busy_timeout` parameter, and adds dedicated leasing (`DB.Lease`, `Lease.Conn`, `Lease.Release`) that configures each leased connection with the exact 64 MiB connection-local SQLITE_LIMIT_LENGTH through `modernc.org/sqlite`'s documented `sqlite.Limit` mechanism before any work runs. Barrier-driven tests prove concurrent callers receive distinct physical connections in pre-placed WAL and rollback-journal fixtures with all per-connection configuration intact, journal mode never set or changed, release cleanup safe on success/error/repeat, and released leases refused for reuse. Created [connection-pool.md](connection-pool.md); updated `sqlite-startup.md`, `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-26] ingest | Issue #6 cancellation infrastructure

Ingested the Issue #6 implementation: `internal/connection/request.go` adds the reusable cancellable request lifecycle (`Request` with process-unique atomic-counter identity, owned derived context, idempotent `Cancel` dispatching exactly one connection-scoped interrupt via the nilable `Lease.interruptFn` seam, visible `running`/`cancelling`/`settled` state persisting until exactly-once `Settle`, cancellation-wins late-success classification, lease release only after settlement through idempotent `Close`, never force-closing a connection). Fake-backed tests cover identity, racing-Cancel interrupt uniqueness, state visibility, the full outcome matrix, and real-pool lease-hold-before-settlement with no-force-close/reuse. Pinned-driver barrier-based capability tests (`interrupt_unix_test.go`, Linux/macOS mandatory and release-blocking) prove modernc v1.57.0 interrupts settle controlled CPU-bound work within one second and SHARED-lock-blocked writes within five seconds, isolate the other dedicated lease, discard deliberately released late success as cancelled, and leave the interrupted connection usable and reusable. Created [cancellation-infrastructure.md](cancellation-infrastructure.md); updated `connection-pool.md` indirectly via cross-references, `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-26] ingest | Issue #7 request-boundary database identity checks

Ingested the Issue #7 implementation and Linux/macOS tests: `internal/connection/health.go` adds typed `HealthKind`/`HealthError` (`HealthDeleted`, `HealthReplaced`) with neutral diagnostics, `VerifyHealth()` comparing the startup-recorded device/inode of the validated target (recorded in `Open`; platform split via `identity_unix.go`/`identity_other.go`), and `RunRequest(parent, op)` as the reusable request boundary — verification inside `Lease` before every request and before any newly opened/replacement pooled connection is admitted for use; post-error reclassification makes deletion/replacement take precedence over ordinary error handling while successful results stand until the next boundary; in-place same-inode mutation stays ordinary SQLite behavior. `Lease` wiring updated in `startup.go`. Barrier-driven `health_test.go` proves classification tables, lease refusal, boundary-before-work blocking with exactly one check, raced replacement followed by error/success, and exactly one pre-BEGIN check for entire phased writes (commit and rollback) with none between statement execution and COMMIT; no watcher/polling/UI dependency and terminal copy remains Issue #46's. Created [session-health.md](session-health.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #8 responsive TUI shell and minimum-size restoration

Ingested the Issue #8 implementation: created `internal/ui` with pinned `bubbletea v1.3.6`/`lipgloss v1.1.0` (no Bubbles components apply to the shell yet). Documented exact region arithmetic (one bottom global footer row; builder desired height including its own border+padding capped at floor(H/3); every remaining row to an independently bordered results region greater than half-height; complete-row page area after results-owned border, status/count, and header rows), focused-field internal scrolling within the capped viewport, the exact 80×24 minimum where `View()` returns exactly `terminal too small`, hidden-state suspension without mutation, ignored ordinary keys, Ctrl+W routing only when hidden state owns active cancellable work, and exact context/focus restoration on return to supported dimensions. Created [responsive-tui-shell.md](responsive-tui-shell.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #9 schema catalog and table eligibility

Ingested the Issue #9 implementation: created `internal/schema` (`schema.go` types + `catalog.go` pure `BuildCatalog` rules) and `internal/connection/schema.go` plumbing through the shared request boundary. Documented main-schema-only queries (`main.sqlite_master` with `type IN ('table','view') ORDER BY name`, bound-parameter `main.pragma_table_xinfo(?)`, same-request `PRAGMA schema_version` capture), ordinary-table/virtual-table/view kinds, write eligibility with SELECT-only views, rowid capability {has-rowid, without-rowid, not-applicable}, declared-rowid-alias shadowing, suffix-based WITHOUT ROWID detection, fts5 shadow tables as ordinary tables, hidden/generated column noninsertability from table_xinfo raw values, declared type as metadata only (no type-specific input behavior), exact `sqlite_%` and `_cf_METADATA` exclusions, and deterministic ascending-name output; refresh failures keep typed health classification and preserve causes for stale-data/retry UI use. Created [schema-catalog.md](schema-catalog.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #10 early integration tracer

Ingested the Issue #9-of-#10 disposable tracer implementation: `internal/schema/tracer.go` (`ChooseTracerTarget` typed catalog selection plus `SelectAllSQL` safely double-quoted hardcoded `SELECT *`), `internal/connection/tracer.go` (`RunTraceSelectAll` executing exactly one quoted SELECT * as one complete `RunRequest`, `TracerResult{Columns, Rows [][]any}` typed transport with preserved-cause neutral failures), and the Bubble Tea composition path (`internal/ui/tracer.go` messages/executor seam with isolated `Model.Trace *TraceView`; rendering in `internal/ui/view.go`). Documented the risk-reduction purpose, Connection–Schema–UI ownership without UI database behavior, typed row/error boundaries (nil/int64/float64/string/[]byte), minimal bordered grid/basic error display inside the Issue #8 shell, and every omitted production feature — builder, validation, pre-execution workflows, WHERE/GROUP BY/ORDER BY/LIMIT, parameter binding, paging, count request, cache, histories, cancellation UX, retry/recovery, and writes. Stated explicitly that Issue #22 must replace the tracer rather than extend it into a production query path; referenced Issue #10 and the Module Design and Testing Decisions in `Notes/PRD-sqloid.md`. Created [early-integration-tracer.md](early-integration-tracer.md); updated `source-code.md`, `unit-tests.md`, and `index.md`. No composition-root change was needed: `cmd/sqloid/main.go` is untouched because no live TUI launch exists yet.
## [2026-08-27] ingest | Issue #11 command and table selection lifecycle

Ingested the Issue #11 implementation: created `internal/querybuilder` (`builder.go` package scope + `command_table.go` lifecycle) with immutable value-level transitions — `NewQuery()` starts unselected with Command focused; each one-key S/U/D/I replacement bumps `DownstreamGeneration()` (all downstream command-specific state discarded), recomputes eligibility from `internal/schema` metadata (`WriteEligible`/kinds, no duplicated catalog rules), retains a selected table only while still eligible for the new command, clears names absent from the latest refresh, and focuses Table; views survive SELECT only and clear for every write command while the refreshed eligible write-table list stays populated with ordinary and virtual tables. Integrated through `internal/ui/command_table.go` (+ `QB` field, `SchemaRefreshedMsg{Catalog}` injection in `model.go`): Command renders at startup, plain letters route only while Command is focused, Shift+Tab revisits Command for replacement, Table appears and holds focus after any selection. Idle results region pinned to exactly `Select a command (S/U/D/I) to begin` with no frozen header/range/count, unchanged layout arithmetic at 80×24/100×30/160×50, and distinction from executed-empty presentation. Red-green test flow documented (pure builder tests + scripted model tests + rendering assertions); tracer remains isolated pending wholesale replacement by Issue #22; searchable popups deferred to Issue #12. Cross-referenced Issue #11 and the Builder and Display Interaction, Builder lifecycle, QueryBuilder Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [builder-command-table.md](builder-command-table.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #12 searchable popup interaction contract

Ingested the Issue #12 implementation: created `internal/ui/popup.go` (reusable searchable/scroll-only popup state: identity-separate candidates, source-order preservation, case-insensitive subsequence matching, exact `no matches` empty/no-match states kept open, deterministic highlight/viewport reset on every search-text change, clamped boundary scrolling, viewport windows, single-select accept, multi-select add-and-reopen with duplicate exclusion and insertion-order `Completed()`), `internal/ui/popup_view.go` (deterministic rendering of the search-input line, highlighted/plain rows, `no matches`, viewport windowing, go-runewidth cell truncation, and `composeOverlay` splicing that never reflows base regions), and the model routing in `popup.go`/`model.go` (`installPopup` capturing exact opener focus, `handlePopupKey` consuming Up/Down/Enter/Esc/printables before base context so nothing leaks into builder shortcuts, close-then-commit accept hooks). Wired Table through `internal/ui/table_popup.go` as the first end-to-end searchable single-select consumer: fresh popups from `QueryBuilder.EligibleTables()` under the selected command's own eligibility rules, acceptance committing object identity via `QB.SelectTable` plus `applyBuilder`, viewport capped at 8 rows, Enter/Esc both restoring the exact opener focus. Documented variants, matching, reset, scrolling, input modality, selection/duplicate/Esc semantics, restoration, rendering, and later column/grouping/aggregate/operator flows as future consumers. Referenced Issue #12 and the Builder and Display Interaction, UI Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [searchable-popups.md](searchable-popups.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #13 stale schema refresh, retry, and terminal precedence

Ingested the Issue #13 implementation: `internal/schema/refresh.go` (typed `Attempt`/`RefreshStatus` with constructor+`Valid()` payload rules; terminals mapped from Connection `HealthError` kinds) and `internal/ui/schema_refresh.go` (`CatalogRefresher` seam invoked only in tea.Cmd, identified `SchemaRefreshSettledMsg`, per-open `issueRefresh`, retry/cancel messages, attempt-identity discards of superseded/late/post-terminal results). Every Table open now issues one fresh catalog request through `beginTablePopup`; ordinary failures retain prior catalogs verbatim behind exactly `Schema data is stale — retry or cancel` plus inline `could not refresh: <cause>` (rendered once per surface — popup body while open, results region after close), block acceptance/navigation/continuation via `ContinuationBlocked()`; Esc under stale cancels the flow restoring exact opener/pre-open state; repeated failure updates only that attempt's cause leaving retry/cancel available; successful retry installs the refreshed catalog and continues the popup via deterministic whole-catalog `Popup.ReplaceCandidates`; typed deletion/replacement transitions to exact `Database file no longer exists/was replaced — session ended` shells, suppressing all controls and rejecting late completions. Created [stale-schema-refresh.md](stale-schema-refresh.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.

## [2026-08-28] ingest | Issue #14 universal value parsing and safe SQL atoms

Ingested the Issue #14 implementation: `internal/querybuilder/value.go` (verbatim untrimmed `ParseValue` grammar — INTEGER `-?[0-9]+` fitting signed int64 first including leading zeros and both boundaries, finite `strconv.ParseFloat` REAL second including hex floats with leading-`+` and overflow fall-through, exact TEXT fallback for whitespace/NUL/typed NULL/nonfinite/hex-integer/injection shapes; `ParamValue()` stable `int64`/`float64`/string bind types with typed NULL and empty input staying strings, no declared-type coercion), `sql_atoms.go` (single-atom double-quote identifier quoting fed only by `internal/schema` object/column identities, closed Operator/Aggregate/Direction enums rendering exact PRD tokens and rejecting invalid values, Predicate assembly binding every user value with deterministic ordering), and `sql_literal.go` (`RenderSQLLiteral`: canonical decimal INTEGER, shortest round-trip REAL with `.0` restoration preserving integral/negative-zero/subnormal/exponent identity and typed non-finite rejection, quote-doubled TEXT preserving emptiness/whitespace/NUL bytes, exactly `NULL`, `X'lowercase-hex'` BLOB — the sole serializer exposed to future Issues #40 and #48 while execution stays on bound parameters). Table-driven red-green tests: `value_test.go`, `sql_atoms_test.go`, `sql_literal_test.go`. Cross-referenced Issue #14 and the Query Grammar, Numeric value parsing and rendering, SQL safety, Query save targeting, and Testing Decisions of `Notes/PRD-sqloid.md`. Created [sql-atoms-and-literals.md](sql-atoms-and-literals.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.

## [2026-08-27] ingest | Issue #15 SELECT wildcard and COUNT(*) projection path

Ingested the Issue #15 implementation: `internal/querybuilder/projection.go` (typed projection candidates with identity separate from display text and reserved-prefix `Key()` encodings so real columns named `*`/`COUNT(*)` never collide with synthetic identities; empty-projection derivation with `*` first/default-highlighted, bare `COUNT(*)` second, then visible columns in Schema order; conditional sentinel hiding once any entry exists and reappearance when removal empties the projection; immutable transitions where wildcard and bare `COUNT(*)` commit directly — sentinel requesting a Column(s) reopen without ever asking for a named-aggregate choice — while named columns return pending aggregate choices that `CompleteProjectionAggregate` turns into `(column, aggregate)` entries with reopen; guard-rejected out-of-range removals) integrated into `builder.go`/`command_table.go` (projecting state on `QueryBuilder`, cleared on command replacement and table-loss refresh, plus the new `FieldColumns`). UI side: `internal/ui/projection_popup.go` opens Column(s) as a searchable single-select popup populated verbatim from builder candidates at 8-row viewport, routes acceptance through builder outcomes — direct sentinel/wildcard commits followed by `reopenColumnsPopup` (exact focus, deterministic reset) only when requested, named columns into the scroll-only Value/Count/Min/Max/Avg/Sum aggregate popup whose completion reopens Column(s) preserving completed entries — and renders committed entries in the new field-bar slot via `command_table.go`; `table_popup.go` routes Enter on focused Column(s). Documented that wildcard and bare `COUNT(*)` never open an aggregate popup and that `MIN(*)`/`MAX(*)`/`AVG(*)`/`SUM(*)` are unrepresentable by construction. Referenced Issue #15 and the Query Grammar, Builder and Display Interaction, QueryBuilder Module Design, UI Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [projection-count-star.md](projection-count-star.md); updated `source-code.md`, `unit-tests.md`, and `index.md`.
## [2026-08-27] ingest | Issue #16 ordered projection editing and deduplication

Ingested the Issue #16 implementation: `internal/querybuilder/projection.go` (`projectionHasWildcard` guarding exclusivity; `AcceptProjection` accepting wildcard from any state as an atomic whole-list replacement focused on Column(s) with no reopen, sentinel re-commits as unchanged no-ops, junk-carrying synthetic identities normalized, malformed empty-name named identities routing nowhere; `CompleteProjectionAggregate` rejecting aggregate completion beside a wildcard and exact repeated `(column, aggregate)` pairs — including `(column, Value)` — as full no-ops without reorder/replace/focus change while distinct appends continue in insertion order across Value/Count/Min/Max/Avg/Sum with same-column distinct aggregates and same-aggregate distinct columns, the bare `COUNT(*)` sentinel coexisting with later named aggregates; new immutable `RemoveLatestProjection()` removing exactly the latest committed entry per call, walking backward through named entries and the sentinel, emptying a sole wildcard, no-op when empty). UI side: `internal/ui/model.go` routes Backspace/Delete from the base Column(s) field (popup-free, non-suspended base context) through that builder transition via `applyBuilder`, keeping focus on Column(s), leaving popup contexts governed by the reusable Issue #12 contract (search backspace edits search only; scroll-only aggregates unaffected), and relying on QueryBuilder candidate derivation so returning to empty restores Issue #15's `*`-first/`COUNT(*)`-second sequence without patching candidates in UI code. Tests: `internal/querybuilder/projection_test.go` (ordered insertion, duplicate rejection, sentinel defense, wildcard atomic replacement/exclusivity, malformed identities, remove-latest contract including rejected-duplicate history) and `internal/ui/projection_editing_test.go` (scripted Backspace/Delete walks through wildcard/named/sentinel states, empty no-op, restoration of empty candidates, and popup-context key scope). Cross-referenced Issues #15/#16 and the Query Grammar, Builder and Display Interaction, keyboard context matrix, QueryBuilder Module Design, UI Module Design, and Testing Decisions sections of `Notes/PRD-sqloid.md`. Created [projection-ordered-editing.md](projection-ordered-editing.md); updated `source-code.md`, `unit-tests.md`, `index.md`, and linked forward from [projection-count-star.md](projection-count-star.md).
