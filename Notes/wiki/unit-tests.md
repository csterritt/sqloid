# Unit and Process-Boundary Test Catalog

Go tests live beside the code they cover. Run with `go test ./...` from the repository root.

## internal/cli

### internal/cli/cli_test.go

In-process, table-driven tests of the shell's routing and output contracts:

- `TestMainRouting` — table of `args → (status, sqlite path, d1 called)`: covers `sqlite <file>` and `d1` dispatch, missing and unexpected arguments, unknown commands, and help/version flags. Handlers are fakes recording dispatches.
- `TestVersionOutput` — `--version` and `-v` write exactly `sqloid <Version>\n` (captured by swapping `os.Stdout` through a pipe) with status 0 and no command dispatch.
- `TestVersionOutputIsSilentOnSuccess` — successful `d1` dispatch writes nothing to stdout.

## cmd/sqloid

### cmd/sqloid/main_test.go

Process-boundary tests asserting the real binary's exact streams and exit statuses. `TestMain` re-executes the test binary as `sqloid` when `SQLOID_CLI_UNDER_TEST` is set, running the CLI with handlers that record dispatches to a file (`SQLOID_CLI_RECORD`):

- `TestCLIStreamsAndStatuses` — table of `args → (status, exact stdout)`: usage failures exit 2, help exits 0, version stdout is exactly `sqloid <version>\n`, and successful `sqlite`/`d1` dispatch is completely silent on both streams.
- `TestUsageFailureWritesErrorAndUsageToStderr` — missing `sqlite` argument: status 2, empty stdout, stderr contains both `Error:` and the usage block.
- `TestHelpGoesToStderr` — `--help` exits 0, lists `sqlite` and `d1`, and writes only to stderr.
- `TestSuccessfulDispatchRunsHandlersSilently` — `sqlite example.db` and `d1` exit 0 with empty streams, and the recorded dispatch lines prove the handlers ran with the routed arguments.

Cross-references: [cli-contract.md](cli-contract.md), [source-code.md](source-code.md).

## internal/cli

### internal/cli/startup_test.go (Issue #2)

Startup-failure rendering with real fixtures through `Main` using the production `connection.Session` handler:

- `TestStartupFailuresRenderOneLineOnStderr` — table over missing, unreadable, directory, invalid-header, and non-writable fixtures: each prints exactly its documented one stderr line, nothing on stdout, and exits 1. Skips under root, where mode-based unreadability cannot be exercised.
- `TestSuccessfulStartupIsSilent` — a valid database exits 0 with completely empty streams.
- `TestStartupFailureKeepsStructuredCause` — startup errors remain `*connection.StartupError`, guarding structured classification for rendering.

### cmd/sqloid/main_test.go — TestSQLiteStartupProcessBehavior

Runs the re-executed test binary with the production connection handler (`SQLOID_CLI_REAL=1` env selects `connection.Session` instead of the recording stub). Asserts silent status-0 success on a valid fixture plus exact one-line stderr diagnostics and status 1 for missing, invalid-header, and non-writable databases — process-level verification of the Issue #2 contract.

Cross-references: [cli-contract.md](cli-contract.md), [sqlite-startup.md](sqlite-startup.md).

## internal/d1

### internal/d1/discovery_test.go (Issue #3)

Table-driven filesystem tests via `t.Chdir(t.TempDir())`: sole-candidate selection with exact joined path; zero candidates across directory-absent/empty/metadata-only/sidecar-only/wrong-case/nested/alternate-layout fixtures (`ErrNoCandidate`, empty path); multiple candidates including uppercase-`Metadata` names remaining eligible under the case-sensitive rule (`ErrMultipleCandidates`); exclusions leaving a surviving top-level candidate while metadata, sidecar, nested, and alternate-layout files are ignored.

## Additional Issue #3 tests elsewhere

- `internal/cli/d1_test.go` — `TestRunD1PassesSoleCandidateUnchangedToSharedOpener` proves the discovery→opener handoff passes the exact path unchanged through injected seams; `TestD1EndToEndOpensSoleDiscoveredCandidate` runs `sqloid d1` through `Main` on a mixed fixture (real SQLite candidate plus ignored metadata, sidecar, wrong-case, nested, and alternate-layout files) asserting silent status-0 success.

## Issue #4 discovery-failure golden tests (internal/cli/d1_test.go)

- `TestRunD1DiscoveryFailureMapsExactDiagnosticAndSkipsOpener` — with each typed `internal/d1` outcome injected: asserts the mapped diagnostic's exact spelling, line count (2 for zero candidates with the expected-path + `sqloid sqlite <file>` hint; 1 for multiple candidates without a hint), and that the shared opener is never invoked.
- `TestD1DiscoveryFailureProcessBehavior` — runs `sqloid d1` through `Main` on real fixtures via `t.Chdir`: missing, empty, and candidate-free Wrangler directories produce exactly the two-line zero-candidate diagnostic; multiple `.sqlite` candidates produce exactly the single line. Each case asserts stderr equality byte-for-byte, silence on stdout, status 1, and — by walking the working directory before and after — that no database or stray file was created.
- `TestD1DiscoveryUnreadableDirectoryProcessBehavior` — an unreadable (`0o000`) Wrangler directory yields the identical zero-candidate two lines with status 1 and no creation. Permission cases skip under root.
- `internal/connection/opener_test.go` — `TestOpenRelativePath` pins read-write mode=rw opening of a working-directory-relative discovered path.

## internal/connection Issue #5 pool and leasing tests

- `internal/connection/pool_config_test.go` — exact-two pool contract: maximum size 2, simultaneous usable distinct leases within an explicit bound, retained floor of two idle connections after release, per-connection five-second busy timeout, and per-connection exact 64 MiB `SQLITE_LIMIT_LENGTH` (cross-checked on every inspected connection).
- `internal/connection/lease_test.go` — dedicated-leasing high-risk coverage from the PRD: barrier-driven concurrent lease pairs in WAL and rollback-journal fixtures prove distinct physical connections, all per-connection invariants hold on each held lease, journal mode is neither set nor changed, and release/reuse safety is enforced.

## internal/connection Issue #6 cancellation lifecycle tests

- `internal/connection/request_test.go` — fake-backed (no live driver) request lifecycle: unique request IDs and context ownership; exactly-one interrupt dispatch under eight racing Cancellers and after settlement; visible `running → cancelling → settled` state where cancelling persists until Settle even after internal work completion; late success discarded as cancelled; error classification matrix with settle-idempotence; lease-hold-before-settlement proven by fast-failing third-lease acquisition on the real two-connection pool; no-force-close plus safe same-lease and released-pool reuse after settled cancellation.
- `internal/connection/interrupt_unix_test.go` — Linux/macOS mandatory barrier-based pinned-driver capability evidence (`//go:build unix`, release-blocking): controlled CPU-bound probe scan settles under one second of cancellation; lock-wait INSERT behind a held SHARED read lock settles within five seconds; independent second-lease isolation during interruption; deliberately released late success classified cancelled and discarded; subsequent harmless PRAGMA work and pool reuse prove the interrupted connection was never force-closed.

## internal/connection Issue #7 request-boundary identity tests

- `internal/connection/health_test.go` — Linux/macOS barrier-driven integration tests (`//go:build unix`): startup records the validated target's device/inode; `VerifyHealth` classification table (unchanged → nil, deletion/rename-away → typed deleted with cause unwrapping to `fs.ErrNotExist`, same-path replacement with a distinct inode → typed replaced, in-place same-inode mutation through an outside connection → nil ordinary behavior); `Lease` refuses admission after deletion or replacement; `RunRequest` exercises schema-style, count-style, and estimate-style reads plus two barrier-rendezvoused pooled requests on distinct physical connections; changed targets block before work with exactly one boundary check and the operation never started; post-error reclassification races (deletion/replacement applied after a successful precheck while a failing request is in flight) take precedence over the preserved SQLite error; raced replacement after success stands until the next boundary detects replacement before work; phased writes (commit and rollback) receive exactly one pre-BEGIN check with none between statement execution and COMMIT.

Cross-references: [cancellation-infrastructure.md](cancellation-infrastructure.md), [connection-pool.md](connection-pool.md), [session-health.md](session-health.md), [sqlite-startup.md](sqlite-startup.md).

## internal/schema Issue #9 schema catalog tests

- `internal/schema/catalog_test.go` — table-driven pure contract tests over synthetic metadata: object kind classification (ordinary/virtual/view incl. odd-case CREATE VIRTUAL TABLE and WITHOUT ROWID suffix variants with semicolon/whitespace/trailing comment), write eligibility, rowid capability plus declared-rowid shadowing for all three aliases (`rowid`, `_rowid_`, `oid`, case-insensitive), generated/hidden columns as noninsertable with declared-type passthrough, zero-insertable tables, exact exclusion of `sqlite_%` (any case) and `_cf_METADATA` while look-alikes survive and indexes/triggers are ignored, ascending-name determinism across input orders, empty-column-map objects, and the human-facing kind/capability strings.
- `internal/schema/catalog_integration_test.go` — SQLite-backed (`//go:build unix`) fixture covering an ordinary table, an fts5 virtual table with its five shadow tables (config/idx genuinely WITHOUT ROWID), a view, a WITHOUT ROWID table, a declared-`rowid` shadowing table, AUTOINCREMENT's auto-created `sqlite_sequence`, `_cf_METADATA`, and a generated-columns mix. Asserts catalog version equals direct `PRAGMA schema_version`; the reserved objects exist in the DB yet never surface in the catalog; per-object kinds, rowid capabilities, shadowing, eligibility sets, virtual-table hidden columns, view SELECT-only zero insertable columns, and column declared types/insertability; repeated reads deep-equal; and DROP raises the version and removes the object from a refreshed catalog.

## internal/connection Issue #9 catalog boundary tests

- `internal/connection/schema_test.go` — success result with populated catalog; two concurrent catalog reads leasing distinct pool connections; cancelled context classified failed with `context.Canceled` unwrappable; same-path replacement at the request boundary yields typed `HealthReplaced` (see [schema-catalog.md](schema-catalog.md)).

## internal/connection and internal/result Issue #22 first-page boundary tests

- `internal/connection/firstpage_test.go` — SQLite-backed: typed NULL/INTEGER/REAL/TEXT/BLOB rows with original driver labels entering `result.Page` without string coercion, typed NULLs, zero rows, an ordinary query failure with a preserved cause and no terminal-classification claim, ordered parameter binding, a pre-cancelled context, and scan-level typing without loss.
- `internal/result/result_test.go` — table-driven contract tests for full-set name deduplication (empty labels, pre-suffixed labels, repeated `COUNT(*)`, collision chains, original slice untouched), finite REAL tokens (integral-looking values, signed zero, exponents, precision edges, locale independence, exact round-trip, INTEGER/TEXT distinction), grid control-character transformation, maximal invalid-UTF-8 replacement to exactly one U+FFFD per maximal subpart, typed-cell display without type coercion, and exact BLOB byte retention including empty and invalid-UTF-8 payloads.
- `internal/result/architecture_test.go` — parsed-source architecture assertions: `internal/result` imports neither Bubble Tea nor any driver; `internal/ui` owns no private REAL/BLOB/UTF-8/deduplication formatting; no tracer execution route survives in production source.
- `internal/querybuilder/firstpage_sql_test.go` — exact safely quoted SQL and ordered parameters for wildcard SELECT, duplicate-label aggregate projections, WHERE value binding order, and GROUP BY/ORDER BY/LIMIT clause assembly.

## internal/connection and internal/result Issue #24 concurrent page/count tests

- `internal/connection/count_overlap_test.go` — mandatory capability suite over WAL and rollback-journal fixtures with the mode recorded before opening: simultaneous barrier-held overlap proving both concurrent requests hold distinct dedicated leases with neither serialized, a third lease blocked while both hold the exact-two pool, distinct physical-connection identities, unchanged journal mode after Open and after concurrent runs, independent-snapshot drift with an interleaved external writer (delayed success or the ordinary `database is locked` error, both accepted), count failure leaving a successful page untouched with typed causes, and clean lease release on success and error. No sleeps: every wait is a channel receive or an explicit timeout bound.
- `internal/result/count_test.go` — exact count wording: pending `Counting rows…`, success `Result count: N`, after-Limit `Result count: N (after Limit M)` (including fewer counted rows), failure `Count unavailable`, and zero-value state rendering nothing.
- `internal/result/select_identity_test.go` — distinct monotonic nonzero execution and request IDs, exact two-level acceptance, wrong-role/stale-execution/duplicate rejection, and swapped-role non-interchangeability; Issue #41 adds fresh monotonic nonzero `NextWriteExecutionID()` values in their own identity space.
- `internal/querybuilder/count_sql_test.go` — exact `SELECT COUNT(*) FROM (<complete SELECT>)` with the user's Limit inside the subquery, preserved bound parameters and order, aggregate/grouped SELECTs, non-SELECT emptiness.
- `internal/ui/concurrent_count_test.go` — scripted model coverage: launch carrying one execution ID and two distinct nonzero role request IDs with exact page/count SQL and ordered parameters, count-before-page and after-page settlement, after-Limit wording with no row clamping, `Count unavailable` isolation with retained rows, page-failure independence, wrong-role/duplicate/superseded rejection, a delayed count from an older execution ignored after a newer SELECT begins, and help naming the complete limited SELECT, independent snapshots/drift, and no clamping.

## internal/ui Issue #8 shell tests

- `internal/ui/layout_test.go` — table-driven pure arithmetic over 80×24, 100×30, and 160×50 with minimal and growing builders: exactly one footer row; builder desired height inclusive of border+padding capped at floor(H/3); results equal every remaining row and greater than half-height; regions partitioning H exactly; `PageRows` subtracting results' owned fixed rows; viewport excluding builder border/padding. `TestViewRegionOwnership` renders full screens asserting exactly H rows with the footer on the last, both boxes occupying exactly their owned heights, exactly two top-border corner rows across the whole view (independent borders, none shared), and page area consistent with inner rows minus status/header.
- `internal/ui/suspension_test.go` — scripted `(model, msg) → (model, cmd)` behavior: focus scrolling with builders far beyond the cap keeping each complete multiline focused field inside the visible range on Tab/Shift+Tab/Up/Down; below-minimum sizes (both-below, width-only, height-only) render exactly `terminal too small`; ordinary keys produce no commands, leave the view exact, and mutate nothing hidden; resize-back restores context/focus/scroll exactly then applies normal layout; Ctrl+W returns non-nil routing into the generic cancellation flow only when hidden state owns cancellable work, invoking it without exposing or mutating hidden state, and returns nil otherwise.

## internal/ui Issue #22 first-select and result-grid tests

- `internal/ui/first_select_test.go` — scripted model coverage of the production route: runnable builder through validation handoff to exactly one executor call carrying the builder's SQL/parameters, history appended exactly at the actual-execution boundary with consecutive-identical suppression, ordinary execution-error settlement, and failed validation producing no execution and no history.
- `internal/ui/results_grid_test.go` — focused view tests over `internal/result` fixtures only: frozen deduplicated header, absolute inclusive `rows 1-N` range, typed cell distinctions (NULL/INTEGER/finite REAL/TEXT/BLOB), Issue #23 non-finite REAL tokens (exact `Inf`/`-Inf`/`NaN` per-row grid tokens beside identical-looking TEXT, finite REAL tokens unchanged, backing REAL and TEXT values retained), visible tab/newline symbols, persistent invalid-UTF warning without extra rows/columns, exact `No rows` distinct from the startup prompt, and the result-error boundary.

Cross-references: [first-select-result-grid.md](first-select-result-grid.md), [non-finite-real-grid.md](non-finite-real-grid.md).

## internal/querybuilder Issue #11 builder tests

- `internal/querybuilder/command_table_test.go` — pure table-driven tests using fixture catalogs of ordinary/virtual table and view objects from `internal/schema`: the initial unselected state (no command, no table, Command focused); one-key selection of each S/U/D/I command advancing to Table without mutating the source snapshot; all sixteen cross-command replacements retaining a selected object exactly when it remains eligible under Schema metadata; downstream generation bumping on every replacement even when the table is retained; view-to-write clearing while ordinary plus virtual tables stay listed as write candidates; refresh-driven eligible-list recomputation (SELECT permissive, writes filtered, vanished names cleared); and `SelectTable` rejecting views during writes, unknown names, and empty input. No Bubble Tea dependency.

## internal/ui Issue #11 command/table tests

- `internal/ui/command_table_test.go` — scripted `(model, msg) → (model, cmd)` tests: startup focuses Command with only that field rendered; each plain S/U/D/I key selects its command and advances both builder and UI focus to Table; Shift+Tab back to Command allows replacement that clears downstream state; letters are inert while Table is focused; injected `SchemaRefreshedMsg` catalogs populate eligibility (three objects under SELECT, two write tables under UPDATE); a selected view clears when INSERT is chosen through the UI path while eligible write tables remain; an eligible ordinary table survives an UPDATE→DELETE switch; and an executed-empty settled result renders differently from idle.
- `internal/ui/view_test.go` — rendering assertions for the pre-execution idle state: exact prompt `Select a command (S/U/D/I) to begin` inside the bordered results region at 80×24, 100×30, and 160×50; status row showing exactly that prompt with every interior row beneath blank (no frozen header, range, or count); no `No rows` or `Result count:` decoration; unchanged region partitioning (exactly H rows at each size); and a settled first-page grid state visually distinct from idle.

## internal/ui Issue #12 popup tests

- `internal/ui/popup_test.go` — pure state tests independent of QueryBuilder and database access: subsequence matching table (case-insensitivity, gaps, order failures, emptiness); empty search showing all candidates including repeated case variants in source order; nonmatching search keeping an open popup with exactly `no matches`; empty candidate data permanently reporting no-match for both variants; filtered ordering across repeated case variants; deterministic highlight/viewport reset on clearing, new searches, and appended runes even when scrolled deep; incremental append/backspace editing; clamped boundary scrolling past both ends of a longer-than-viewport list with the highlight always inside the visible window; scroll-only ignoring all search input; unwindowed defaults; single-select accept/no-match/empty Enters; multi-select add-reopen-duplicate-unique behavior; completed selections surviving no-match filtering plus reset checks; Esc preserving only completed selections; and Enter accepting whatever the caret points at after independent scrolling and filtering journeys.
- `internal/ui/popup_view_test.go` — rendering tests: searchable layout with `Search: _` input line and `> `/plain prefixes; exact `no matches` replacing stale candidates; views reflecting the reset after a changed search (`Search: gam_`, `> gamma`); windows exposing exactly the visible slice of a longer list without leaking off-screen items; scroll-only rendering without any search input; and `composeOverlay` leaving every untouched base row byte-identical.
- `internal/ui/popup_lifecycle_test.go` — scripted `(model, msg) → (model, cmd)` lifecycle coverage through generic popup routing: single-select Enter accepting, closing, committing via the accept hook, and restoring the exact opener focus index; printable keys (including S/U/D/I and `?`) becoming search text without touching command selection, builder focus, or Tab navigation; Backspace/space editing semantics; Esc cancel closing unchanged with exact opener restoration; multi-select add-and-reopen staying open between completions with unique insertion-order results and Esc installing nothing afterward; scroll-only variants navigating through Update without accumulating search; end-to-end Table integration tests opening fresh eligible-list popups per selected command from a refreshed Schema catalog, narrowing to `users` under UPDATE with search, committing object identity through QueryBuilder transitions with exact Table-opener focus on both Enter and Esc paths, an empty-catalog popup stuck in an open no-match state where Enter is inert until Esc restores focus, and View-level overlay composition keeping total rendered height identical to the popup-free shell while containing the search line and candidates.

## Issue #13 stale-schema refresh, retry/cancel, and terminal-precedence tests

- `internal/schema/refresh_test.go` — pure attempt-contract tests over an immutable prior catalog: `Valid()` table (success carries its catalog and only success does; ordinary failure carries a preserved cause with no catalog so consumers cannot partially replace; deletion/replacement terminals carry neither; unsettled zero status is invalid), constructor outcomes (`NewFailure` preserves the cause verbatim, successful attempts expose exactly the settled catalog), and the classification strings.
- `internal/ui/stale_schema_test.go` — scripted fake-refresher model tests for refresh-before-presentation and stale retention: every Table open issues exactly one fresh identified request whose settled result replaces presented candidates; reopen always re-requests and presents only the newly refreshed list with refreshed objects installed verbatim (not copied); ordinary failure retains prior candidate identities/order, prior-object pointer identity in eligible lists, pre-existing selected table, and usable search state through the failure; both indicators render exactly once (`Schema data is stale — retry or cancel` plus inline `could not refresh: <cause>`), survive resize cycles and typing, keep deterministic rendering; Enter acceptance is blocked (selection unchanged, focus pinned, popup retained), Tab/Down navigation blocked under the flow, and `ContinuationBlocked()` gates continuation/execution while stale, with Esc ending the flow once cancel semantics exist by restoring opener focus.
- `internal/ui/stale_schema_lifecycle_test.go` — retry/cancel lifecycle and typed terminal precedence: retry issues a fresh request through the same seam while keeping the unchanged catalog and both exact indicators pending, gating duplicate retries; successful retry clears indicators atomically, continues the popup with refreshed candidates, restores continuation, and accepts immediately with exact opener restoration; repeated failure keeps candidates/prior object identity, swaps the inline cause to exactly that attempt's cause, leaves retry available; cancel closes only the stale flow restoring exact Table opener, selected table, command, and catalog snapshot with no continuation or execution started; injected typed deletion/replacement terminals render the whole view as exactly `Database file no longer exists — session ended` / `Database file was replaced — session ended`, suppress stale indicators/controls, neutralize retry/cancel and any key-driven database work; post-error reclassification beats an ordinary cause with no `could not refresh` line; and late or superseded completions are rejected after cancel and inside terminal states without mutating catalogs or reviving popups. Precedence uses structured attempts only — never error-string matching — with no sleeps and no database access.

## internal/querybuilder Issue #14 value-parsing and SQL-atom tests

- `internal/querybuilder/value_test.go` — table-driven classification of the universal parser: verbatim INTEGER recognition (`-?[0-9]+` fitting signed int64, leading zeros, negative zero, both boundaries), int64 overflow falling through to exact float64 REAL values via bit-level comparison, finite REAL forms (decimal, trailing/leading dot, exponent, hexadecimal floats like `0x1p2`/`0x1.8p1`), leading-plus rejection at both stages, non-finite spellings and float overflow as TEXT, whitespace in every position, empty text, typed NULL, and injection-shaped input. A second table pins exact bound dynamic types and values (`int64`, `float64`, string) from `ParamValue()`.
- `internal/querybuilder/sql_atoms_test.go` — safety tables: atom-by-atom identifier quoting through `schema.Object`/`schema.Column` identities (empty-looking, spaces, keywords, qualification-shaped, punctuation-heavy, embedded quotes, injection names); exhaustive fixed tokens for all nine operators, five aggregates, and two directions with invalid zero/out-of-range rejections returning empty tokens; predicate rendering with `'?'` placeholders, one deterministic bound parameter matching the parsed value's unchanged type, no user-value leakage into SQL text, and IS NULL / IS NOT NULL binding nothing.
- `internal/querybuilder/sql_literal_test.go` — exact standalone literal tokens: canonical decimal INTEGER at int64 boundaries; REAL identity preservation including integral `.0` restoration, sign-bit-tested negative zero, subnormals, exponent output, and typed non-finite rejection; quote-doubled TEXT preserving emptiness, whitespace, NUL bytes, and injection content; exactly `NULL`; uppercase-X lowercase-hex BLOB including empty payloads; unsupported kinds rejected; and `Value.Literal()` conversion feeding parsed results into the renderer without a second parse.
- `internal/export/save_target_test.go` (Issue #48) — table-driven Ctrl+S target resolution: every ordinary pairwise and all-present combination among the viewed-result query, runnable builder, and last execution plus absent and non-runnable builders; evicted-association fall-through to the next candidate; terminal-only priority from the Ctrl+P/N-selected immutable query to the last actual execution with the builder and viewed-result candidates deliberately supplied and ignored; the exact `no runnable query to save` typed error; and resolved-state immutability (mutation of the resolved copy never reaches retained history).
- `internal/export/sql_serialize_test.go` (Issue #48) — exact standalone statement bytes across SELECT variants (plain columns, wildcard, aggregated and grouped, ORDER BY column/aggregate/COUNT(*) with direction and Limit, IS NOT NULL), UPDATE SET ordering with Value/NULL choices and optional WHERE, unqualified and qualified DELETE, INSERT Value/NULL/Default-Omit choices and all-omit DEFAULT VALUES; difficult schema-derived identifiers (embedded quotes, keywords, punctuation, empty), empty/injection-looking TEXT, signed int64 boundaries, REAL integral identity/negative zero/exponent/subnormal/precision edges; exactly one trailing semicolon and no placeholder; and modernc SQLite round trips proving quote-doubled TEXT, SQL NULL with default application for omitted INSERT columns, signed int64 and REAL shortest-round-trip identity, difficult-identifier execution, unqualified/qualified UPDATE/DELETE row effects, and empty/non-empty BLOB byte-for-byte through the typed atom.
- `internal/export/export_capture_test.go` — Issue #49 capture/eligibility coverage: synchronous deep-capture immutability (post-capture mutation of labels, row slices, cells, original BLOB bytes, and appended rows leaves the capture byte-identical), deduplicated names, ascending one-based positions with unanchored defaulting, the pure eligibility matrix with exactly the one shared `selected result has no tabular data to export` definition, and the serializer-spy proof (structural `Payload` field pin plus behavioral absence of any warning/outcome/completeness string from names and cell tokens).
- `internal/ui/export_capture_test.go` — scripted Ctrl+X targeting: idle-active capture with full active-SELECT/request/generation/state preservation, returned-nil command, and zero executor calls; live-mutation immunity across cells, BLOB bytes, and page slices; historical capture of the selected retained snapshot with post-capture selection changes not following it; terminal targeting in deletion, replacement, and outcome-unknown workflows following Ctrl+E/Y selection changes with zero database work and undisturbed terminal state.
- `internal/ui/export_gating_test.go` — table-driven Ctrl+X gating during schema validation, schema refresh, estimate, SELECT first/later page, count-only work, cancellation settlement, and write beginning/executing/rollback-cleanup/committing (exact `export is unavailable while a request is in flight`, no capture, no picker, unchanged fake call counts); the non-tabular rejection table (nothing displayed, error view, selected error/cancelled-before-rows/write-summary/outcome-unknown entries, evicted selection) with exactly the shared rejection and no capture/picker; and retained-row failed plus zero-row tabular snapshots remaining exportable controls.
- `internal/ui/export_warnings_test.go` — the metadata-to-warning matrix in canonical order (exclusive complete, partial, truncated, partial+truncated, row-cap, byte-cap reusing Issue #31's exact definition, cancelled/failed with reasons and failure positions, invalid UTF, all-truthful combination); the pre-destination flow from active, historical, and terminal openers with warnings rendered before destination selection, payload/metadata separation, exact opener-fingerprint restoration after both Esc cancel and successful completion, and zero commands.

- `internal/ui/sql_save_targeting_test.go` — model coverage of Ctrl+S targeting: the ordinary priority table (viewed-result query, runnable builder, last execution, pairwise/all-present combos, non-runnable builder skip), the real end-to-end viewed-result association from one executed SELECT's finalization, the Issue #27 in-flight gate still owning Ctrl+S, all three terminal states (outcome-unknown and both health terminals) with selected-query priority and last-execution fallback, exact no-target feedback with no picker and no prepared target in ordinary and terminal states, and zero fake select/count/page/refresh calls across every resolution path.

## Issue #17 WHERE predicate tests

- `internal/querybuilder/predicate_test.go` — table-driven pure coverage: eligible-column derivation per consumer (visible declared-order columns for SELECT/UPDATE/DELETE, none for INSERT or missing tables, hidden excluded); the closed deterministic nine-operator list unchanged by drafting; null operators completing immediately after choice with exact SQL, no placeholder, nil params, and stale submitted values discarded; a full column × value-operator matrix asserting incomplete-until-submission and then exactly one `'?'` plus the parsed bound value at its concrete dynamic type — INTEGER (including min-int64), REAL, typed/lowercase `NULL`, empty input, whitespace, `'%'`, `'_'`, mixed wildcards, injection-shaped TEXT — with typed NULL completing end-to-end through StartWhere → ApplyWhereDraft → CommitWhereDraft binding the verbatim string; LIKE values byte-for-byte and absent from SQL; four-state accessor distinctions; receiver immutability per transition; safe atom quoting for punctuation-shaped names; identical SQL/params across all three consumers; defensive rejections (no table, INSERT, unknown/hidden/empty identities, invalid operators, submissions outside awaiting state, committing incomplete or idle drafts, idle cancel, downstream clearing).
- `internal/ui/where_flow_test.go` — scripted end-to-end walks through Update for every consumer: searchable single-select column popup over id/email/note; scroll-only operator popup containing all nine tokens in order; IS NULL / IS NOT NULL completing without any value prompt, closing popups, showing rendered SQL in the focused Where field, and binding nothing; every value-taking operator requiring submission to commit int64(42) exactly; typed `NULL` and empty input binding as verbatim TEXT through the UI; `%a_b%` reaching params byte-for-byte under `"note" LIKE ?`; Esc restoring the prior completed predicate (-5.25 REAL untouched) from every staging stage with prompt/draft state fully discarded and exact opener focus; Shift+Tab round-trip revisit restoring exact prior text (cursor at end-of-buffer) and pre-highlighting the previously chosen operator.
- `internal/ui/where_view_test.go` — view assertions: the Where field renders blank until commitment with its label present for S/U/D; the open value prompt shows the exact inline typed-NULL hint plus both contextual help lines; the prompt overlay changes no line count or width versus the closed frame (no reflow); and Tab cannot move builder focus while either the column popup or the value prompt owns the context.

## Issue #37 UPDATE construction and prompt-restoration tests

- `internal/querybuilder/update_test.go` — pure table-driven coverage for refreshed visible-column acceptance, hidden/unknown rejection, duplicate no-op and first-selection order, immutable Value submission, safely quote-doubled table/column atoms, all-Value/all-NULL/mixed SQL, empty and typed-`NULL` TEXT values, optional value-taking and null-operator WHERE, exact SET-Value-then-WHERE parameter order, and no executable request from incomplete state.
- `internal/ui/update_flow_test.go` — scripted Bubble Tea coverage for searchable SET multi-selection with duplicate attempts, exact scroll-only Value/NULL order, universal Value entry and sequential Where continuation, restored choice highlight/text/cursor/concrete parsed kind, Tab/Shift+Tab and Up/Down assignment revision, cancellation without partial commits, Value-to-NULL-to-Value transitions, empty TEXT, and current-assignment Backspace/Delete whole-value clearing.

Cross-reference: [update-assignment-builder.md](update-assignment-builder.md).

## Issues #40–#41 destructive preparation and estimate lifecycle tests

- `internal/querybuilder/estimate_sql_test.go` — exact standalone rendered UPDATE/DELETE SQL across mixed/all-Value/all-NULL SET assignments, typed TEXT `NULL` and empty TEXT literals, REAL/INTEGER canonical tokens, quote-doubled unusual identifiers, value-taking and null-operator predicates, and unqualified forms; command/runnable gating returning empty; exact `SELECT COUNT(*) FROM <quoted target> [WHERE <identical predicate>]` estimate SQL with SET fragments proven absent; and WHERE-only `EstimateParams()` (nil for no-WHERE and IS NULL/IS NOT NULL, SET values never included, identical to `WhereParams()`).
- `internal/connection/estimate_integration_test.go` (unix build) — SQLite-backed `ExecuteEstimate` through `connection.Open`: qualified counting with the exact WHERE value, the unqualified zero-parameter form over every row, the read-only guarantee verified by re-counting fixture rows, surfaced missing-table failures, and cancelled-context classification.
- `internal/ui/destructive_prep_test.go` — scripted Bubble Tea coverage driving runnable validated UPDATE/DELETE through Enter → `PreExecutionRequestedMsg` → settled validation into the preparation modal: unique preparation identity and exactly one estimate request with exact estimate SQL and WHERE-only params (SET values excluded), continuously visible operation/table/canonical SQL and the exact `Estimating matching target rows…` status through pending states, unrelated resize, and dispatch; the prominent `WARNING: no WHERE clause — every row` warning only for no-WHERE statements with no false warning on qualified writes; disabled Enter/y with no history while pending; retained success (`Estimated matching target rows: N`) and failure (`Estimate failed: <cause>`) preserving SQL/warning; stale-identity response rejection followed by current-settlement retention; Esc dismissal cancelling the in-flight request and restoring opener focus; Ctrl+W exact `cancelling…` with cancelled-settlement dismissal; zero query/result history across open, success, failure, and dismissal; and settled Enter asserted to emit exactly one `WriteConfirmedMsg` with no history append.
- `internal/ui/destructive_prep_confirmation_test.go` — Issue #41 scripted confirmation and settlement-guard coverage: settled success and settled failure across qualified/unqualified UPDATE/DELETE showing the count or the error with operation/table/SQL/warning retained and the exact `Enter/y confirms the write — Esc/n dismisses` footer; Enter/y each emitting exactly one command producing `WriteConfirmedMsg` with the confirmed operation, retained rendered SQL, and a fresh nonzero execution identity monotonic across confirmations; confirmation rejected while pending, cancelling, or dismissed (never allocating an execution identity); exactly-once confirmation under repeated Enter/y, duplicate/stale estimate settlement, and `WriteConfirmedMsg` re-delivery before and after command delivery; duplicate/stale settlement messages unable to replace retained estimates/failures, dispatch commands, or mutate either history; repeated Ctrl+W idempotency with stale `CancelEstimateMsg` and late stale estimates inert against the waiting preparation, late success after cancellation creating no execution; and Esc/n/cancel-then-settle dismissal restoring the exact captured opener (focus, scroll, deep-equal builder state) with zero execution and zero history.

Cross-reference: [destructive-preparation.md](destructive-preparation.md).

## internal/querybuilder Issue #15 projection tests

- `internal/querybuilder/projection_test.go` — pure table-driven projection tests using typed `internal/schema` columns with no Bubble Tea dependency: empty-projection candidate ordering (`*` first, synthetic bare `COUNT(*)` second, visible named columns after both with hidden columns excluded) with exact display texts; sentinel acceptance committing directly, reopening Column(s), focusing `FieldColumns`, and never requesting a named-aggregate choice; sentinel/wildcard hidden from candidates once any entry exists and restored (sentinel in second position) when removal empties the projection; wildcard accepted directly as the sole projection leaving zero further candidates; named-column continuation preserving chosen identity and returning pending aggregate choices without committing, from both empty and populated states, with aggregate completion appending the entry and reopening; zero-visible-column tables still deriving exactly the two synthetic candidates; real columns named `*` and `COUNT(*)` keeping distinct identities despite colliding displays; rejection of `MIN(*)`, `MAX(*)`, `AVG(*)`, `SUM(*)` requests over any column including wildcards so aggregate-on-wildcard is unrepresentable; and out-of-range removal indexes ignored. Issue #16 adds pure ordered-editing coverage: insertion order preserved exactly across Value/Count/Min/Max/Avg/Sum with same-column distinct aggregates and same-aggregate distinct columns; exact repeated `(column, aggregate)` pairs — including the zero Value aggregate — rejected as full no-ops (entries, reopen, pending, focus untouched) while later distinct appends continue at the end; bare `COUNT(*)` coexisting with named aggregates and a direct duplicate-sentinel transition being an unchanged no-op from sentinel and mixed states; wildcard replacement from a populated state clearing all prior entries atomically, blocking sentinel and named appends beside it until removal, and re-acceptance staying sole; malformed identities (unknown kinds, junk-carrying synthetic identities) normalized or rejected without bypassing invariants while a real column named `COUNT(*)` still coexists with the committed sentinel; and `RemoveLatestProjection` removing exactly the latest entry per call (walking backward through named entries and the sentinel), leaving earlier order intact, emptying a sole wildcard into restored `*`/`COUNT(*)` candidates, no-op when empty, and never treating rejected duplicates as removable history.

## internal/ui Issue #16 projection editing tests

- `internal/ui/projection_editing_test.go` — scripted model coverage of Backspace/Delete editing driven end-to-end through Update: setup commits `COUNT(*)` then id(MIN) then email(Value) via the popups themselves before returning to the base Column(s) field; repeated rounds remove exactly the latest committed entry walking backward through named entries and the bare sentinel, preserving order and focus on Column(s), with an extra round proving an empty-state true no-op; removing the sole wildcard empties the projection entirely so reopening Column(s) shows the Issue #15 `*`-first and `COUNT(*)`-second candidate sequence; and inside a focused searchable Column(s) popup or the scroll-only aggregate popup the same keys follow the reusable popup contract — search text edits/restores without any committed-entry deletion, popup mode/opener/candidates stay unchanged, and removal never closes, reopens, reorders, or corrupts an open popup.

## internal/ui Issue #15 popup flow and rendering tests

- `internal/ui/projection_popup_test.go` — scripted `(model, msg) → (model, cmd)` end-to-end coverage driven through Update: Enter on a focused Column(s) field opening the searchable single-select popup (`*`/`COUNT(*)`/columns ordering, wildcard as single default-highlighted row via caret markers); bare `COUNT(*)` acceptance committing the sentinel directly and immediately reopening Column(s) with exact focus, deterministic search reset, no synthetics among now-nonempty candidates, and `COUNT(*)` rendered in the field bar; named-column selection from empty and populated projections routing to the scroll-only Value/Count/Min/Max/Avg/Sum aggregate popup without early commits, whose Avg acceptance reopens Column(s) preserving completed entries with the exact `(column, AVG)` identity appended; wildcard acceptance completing the sole projection directly with no aggregate popup ever opened; and removal back to an empty projection through the existing `applyBuilder` seam restoring the sentinel in second position on reopen.
- `internal/ui/projection_view_test.go` — focused rendering assertions: empty-projection row order exactly [`*`, `COUNT(*)`, column] with the wildcard highlighted; aggregate popups scroll-only rendering every choice without a `Search:` line and none of `MIN(*)`/`MAX(*)`/`AVG(*)`/`SUM(*)`; and sentinel-like columns named `*` or `COUNT(*)` rendering their own rows below the synthetic pair.

## Issue #18 GROUP BY, ORDER BY, and LIMIT tests

Pure QueryBuilder coverage in `internal/querybuilder/group_by_test.go` (candidate derivation from visible Schema columns requiring SELECT-with-table, acceptance-order preservation with duplicate/hidden/empty rejections, stale-identity rejection after refresh, command/table replacement clearing, the full grouping validity matrix over nonaggregate-only, mixed (valid and one-missing-group), extra-groups, all-aggregate, bare `COUNT(*)`, and wildcard rows with exact first-invalid field/reason pairs, first-invalid absence when clean, GROUP BY SQL rendering with embedded-quote escaping, and projection-order/parameter preservation), `internal/querybuilder/order_by_test.go` (ungrouped Schema-order candidates, grouped-context restriction to grouped columns plus selected aggregates, bare `COUNT(*)` inclusion, equal-label key distinctness, wildcard/unselected-aggregate exclusion, stale committed selection after context change, arbitrary/stale key rejection, ASC default with atomic replacement, toggle/clear seams, exact SQL rendering including the sentinel and quote escaping, and no-params/projection-order preservation), and `internal/querybuilder/limit_test.go` (empty/unbounded, `1`, MaxInt64, zero in multiple representations, negatives, leading `+`, whitespace, decimal/exponent/hex, nonnumeric text, overflow, extremely long input, revision valid→invalid/empty, canonical rendering, and the exact `Limit must be an integer from 1 to 9223372036854775807` reason with preserved entered text and no runnable SQL — see [group-order-limit.md](group-order-limit.md)).

Scripted UI coverage in `internal/ui/group_by_test.go` (selection order via commit-and-reopen with exhausted candidates, Esc cancel without commit, Backspace removing the latest group), `order_by_test.go` (ungrouped/grouped/sentinel popup candidates, acceptance committing and restoring focus with ASC default content, Esc cancel, committed Up/Down toggling without popup or focus movement, uncommitted navigation, replacement resetting to ASC, Backspace whole-value clear with reselection), and `limit_test.go` (prompt seeding with the prior value, canonical submission preserving the entered representation, invalid submission showing the exact reason verbatim in field and view, Esc restoring the prior value, Backspace whole-value clearing, and empty input as unbounded with no LIMIT clause).

## Issue #19 authoritative runnable-state feedback tests

Pure QueryBuilder coverage in `internal/querybuilder/runnable_test.go` — exhaustive UI-independent table tests over every SELECT, UPDATE, DELETE, and INSERT prerequisite plus the common gates, in each command's visual builder order: missing command, absent/stale table, empty SELECT projection, open WHERE drafts at column/awaiting-value stages, committed WHERE with submitted empty TEXT, the grouping matrix (mixed, wildcard-grouped, stale group), stale ORDER BY after refresh, empty/valid/invalid/zero/overflow Limit with the exact reason, UPDATE no-assignments, duplicate SET columns, incomplete {Value, NULL} choices, unsubmitted Value entries, NULL versus typed TEXT `NULL`, optional WHERE, DELETE absent/complete/incomplete WHERE and `IS NOT NULL` completion, INSERT zero insertable columns, unbegun prompts, incomplete later choices, unsubmitted entries, submitted empty TEXT, all-omit validity — with every multi-failure case returning only the earliest visual field and one specific reason — and in `internal/querybuilder/whole_value_test.go` (immutable whole-value clearing: WHERE for all three consumers preserving column/operator as an incomplete draft, absent/null-operator/open-draft no-ops, valid and invalid Limit restoring unbounded validity, empty Limit no-op, and UPDATE/INSERT Value clearing keeping the choice while dropping the submission).

Scripted UI coverage in `internal/ui/whole_value_clearing_test.go` (Backspace and Delete on the focused base Where field clearing the entire value atomically for SELECT and DELETE with no command, empty-field no-ops preserving focus and state, Limit whole-value clearing with both keys restoring unbounded validity, popup/value-prompt keys retaining their own editing context, Issue #16 projection removal untouched, and stale reasons vanishing after correction) and `internal/ui/runnable_feedback_test.go` (Enter on a multi-failure SELECT consuming the key, moving focus to Column(s) as first invalid in visual order with the exact inline reason and no command; invalid Limit focusing Limit with exactly `abc — Limit must be an integer from 1 to 9223372036854775807` and no prompt reopen; representative duplicate-SET, incomplete-WHERE DELETE, and zero-insertable INSERT states focusing the write-field targets; runnable data emitting exactly the `PreExecutionRequestedMsg` seam; and popup, value-prompt, stale-overlay, pending-request, and too-small contexts consuming Enter with their own behavior).

## Issue #20 minimal query-history append tests

Pure storage coverage in `internal/history/query_store_test.go` (empty-store determinism, stable nonzero non-positional IDs surviving bulk eviction, chronological `Entries()` order, `Lookup` by stable ID with zero/unknown not-found, exact `Capacity == 20` fill then per-append oldest-first eviction preserving all surviving IDs, repeated identical payloads retaining distinct entries, and defensive deep-copy behavior — mutating the source state after append or any retrieved value from `Entries`/`Lookup` cannot alter retained entries), and in `internal/history/query_append_test.go` (A→A consecutive suppression returning `(0, false)` with one retained entry, suppressed appends consuming no ID and never evicting at capacity, A→B→A retaining both A entries with distinct IDs, representative significant-difference pairs appending, and the failed-execution contract that a start append persists after later lifecycle events). Normalized comparison coverage in `internal/querybuilder/history_state_test.go` (significant-field preservation on a rich state, transient exclusion of open drafts and leading-zero entered Limit, table-driven difference matrix over command, table identity, projection order, WHERE presence/column/operator/entered representation/bound type, group order, ORDER BY expression/direction, Limit empty-vs-number and number, plus UPDATE/INSERT ordered choices, entered-representation significance, SQL-NULL versus typed-`NULL` distinction, and deep-copy snapshots). Narrow scripted UI coverage in `internal/ui/query_history_append_test.go` (runnable Enter emitting only `PreExecutionRequestedMsg` with zero appends, the seam message appending once then suppressing the identical consecutive start, A→B retaining distinct IDs through later lifecycle events, and UPDATE never appending without confirmation) — see [query-history-append.md](query-history-append.md).

## Issue #21 pre-execution validation tests

Pure outcome coverage in `internal/schema/revalidate_test.go` (unchanged reuse of the exact prior pointer without invoking refresh, changed refresh installing the new catalog, ordinary failure carrying only the cause with the prior cache standing, terminal deleted/replaced kinds, constructor payload rules, and human-readable status strings), in `internal/querybuilder/revalidate_test.go` (vanishing/eligibility-losing objects dropping the table and downstream state, rowid-property changes invalidating only ORDER BY, individually removed vanished projection/group/WHERE/ORDER BY/SET/INSERT identifiers, unrelated Limit and surviving entries preserved, cleared flags, deterministic post-repair first-reason reports, and unchanged no-ops), and in `internal/connection/revalidate_test.go` (fake-Connection and `modernc.org/sqlite` coverage of `ReadSchemaVersion` under cancellation, health classification, and DDL races). Scripted UI coverage in `internal/ui/validation_workflow_test.go` — see [schema-validation-workflow.md](schema-validation-workflow.md).

## Issue #25 serialized vertical paging tests

UI-independent table-driven coverage in `internal/querybuilder/page_sql_test.go` — exact `LIMIT n OFFSET m` ranges with the page limit clamped to the remaining user Limit, offset-beyond-Limit and invalid ranges yielding empty, WHERE parameter-order preservation, the single eligible implicit `ORDER BY rowid` fallback (ordinary rowid table, no declared rowid/_rowid_/oid shadow, no user ORDER BY, no aggregate/group shape), byte-for-byte user ORDER BY ASC/DESC for plain and grouped-aggregate expressions without an appended rowid tie-breaker, aggregate-only/grouped no-fallback, every excluded object category (view, virtual table, WITHOUT ROWID, all three declared-rowid shadows) as an explicit no-fallback case, ineligible shapes, and `PageParams()` equal to `SelectParams()` — see [serialized-vertical-paging.md](serialized-vertical-paging.md).

Scripted model coverage in `internal/ui/vertical_paging_test.go` — adjacent Page Down/Up ranges issued exactly once through the page seam with the layout-derived complete-visible-row size, low/high/user-Limit boundary suppression, held-pending responses proving repeated and opposite key consumption with no additional request and `loading next page…` feedback, local horizontal movement staying local, count-plus-one-page overlap with exact count wording and retained loading feedback, and page sizes 11/15/34 at 80×24/100×30/160×50 with a resize changing the next request — see [serialized-vertical-paging.md](serialized-vertical-paging.md).

SQLite-backed coverage in `internal/connection/page_test.go` — adjacent disjoint ranges, offset beyond the data as a typed empty page, and ordinary failure classification with the driver cause preserved through the `firstPageError` chain.

## Issue #27 in-flight gating and read-phase feedback tests

Scripted model coverage in `internal/ui/inflight_gating_test.go` — table-driven gating with first-page, later-page, and count work independently held against the fake executor seams: every blocked action (Enter, Ctrl+P/N, Ctrl+E/Y, Ctrl+S, Ctrl+X) consumed with exact feedback, no command, and unchanged request counts; Enter hints carrying the current phase wording plus `press Ctrl+W to cancel`; horizontal `,`/`.` keys staying local with cleared stale feedback; `q`/Ctrl+C opening the shared quit confirmation with printable-key consumption, exact-focus Esc restoration, and Enter/y/Ctrl+C confirmation; Ctrl+W dispatching cancellation exactly once with `cancelling…` when cancellable and being ignored otherwise; terminal → quit-confirmation → top-overlay → focused-input → gate → base precedence; flag-driven gating without rendered labels; settlement release in either arrival order with stale-notice clearing; and count failure keeping exactly `Count unavailable` — see [in-flight-gating.md](in-flight-gating.md).

Focused model and rendering coverage in `internal/ui/read_feedback_test.go` — exact `Running…` while first-page work is held, `Counting rows…` with retained rows while the count is held, distinct `loading next page…` during later-page work, label updates as page and count settle in either order ending at `Result count: 7`, feedback surviving permitted Tab navigation, the `cancelling…` handoff rendering beside pending wording and clearing at full settlement, action-specific rejection strings, popup Enter consumed before the gate, and an explicit guard that no write-phase label appears on any read path (Issue #44 owns those) — see [in-flight-gating.md](in-flight-gating.md).

## Issue #29 whole-column horizontal scrolling tests

- `internal/ui/horizontal_layout_test.go` — table-driven pure layout coverage: wide/narrow terminal packing, layout restarting at the selected first-visible index with recomputed widths, exact-fit boundaries, no room for another complete column, Unicode double-width packing, oversized-first-column capping to the available cell area, grid-cap columns still packing followers, index clamping at both ends, empty results, idempotent recomputation with no intra-cell offset exposed, `horizontalStep` boundary stepping, and `clampFirstColumn` resize normalization.
- `internal/ui/horizontal_keys_test.go` — scripted model coverage of all four bindings: exact one-column moves (`,`, `.`, Shift+Page bridge) repeated across presses, boundary no-ops at both ends, no command and unchanged page-request counts, local movement during held first-page/later-page/count requests, precedence consumption in quit confirmation/popup/focused input/too-small/terminal contexts, rendering that hides the abandoned first column and reveals the newly visible one after a move on a narrow viewport, and resize preservation plus clamping (beyond-last, single-column, empty results).

## Issue #28 scoped SELECT cancellation tests

Barrier-driven handle coverage in `internal/connection/select_cancellation_test.go` (independent page/count cancellation with per-request interrupt identities and settlement ordering, idempotent repeated/late `Cancel` with exactly one settled classification, count-only scope leaving both pool connections healthy) and real-driver capability evidence in `internal/connection/select_cancellation_capability_test.go` (`//go:build unix`, release-blocking: distinct leased connections with probe-proven CPU-bound work, one-second page/later-page/count interruption, count isolation during page cancellation, no lease reuse before every targeted request settles, five-second busy-timeout lock-wait settlement with a 50 ms canary proving the block, cancellation-wins late-success discard, and healthy subsequent work on each interrupted pool — see [scoped-select-cancellation.md](scoped-select-cancellation.md)).

Scripted model coverage in `internal/ui/select_cancellation_test.go` — first-page+count, count-only, and later-page-only Ctrl+W scopes: one independent cancellation per active request, exact `cancelling…` until every targeted request settles, Enter and page keys consumed with no replacement dispatch before settlement, idempotent repeat Ctrl+W, inert cancellation-classified late results through the Issue #26 guards, preserved rows/cache/range metadata, and healthy replacement executions accepted after all-request settlement.

## Issue #30 positional result cache tests

Pure table-driven coverage in `internal/resultcache/cache_test.go` and `cap_test.go`, documented in [positional-result-cache.md](positional-result-cache.md): positional identity for duplicate-valued rows, initial insertion, forward append and backward prepend in ascending order, exact/partial/whole-range and edge-spanning overlap replacement, repeated overlap, alternating traversal, atomic low/high-side gap rejection with unchanged state, empty-page and zero-value rejection, and cap traversal beyond 10,000 positions in exact-cap, one-past, multi-page, and oversized-page shapes with deterministic opposite-end eviction, overlap replacement near each edge under cap pressure, alternating direction after eviction, atomic stale-page rejection after eviction, and per-merge assertions of exact range metadata, ascending gap-free positions, and no duplicates.

## Issue #34 active-SELECT lifetime and exactly-once finalization tests

- `internal/ui/active_select_lifecycle_test.go` — the table-driven lifecycle matrix over count-pending, page-pending, and settled first-page combinations: every non-finalizing event category (idle periods, builder focus edits, help/overlay open-close, save and export keys, query-history browsing keys, gated result-history keys, resize, page settlement, count settlement, count failure, serialized paging dispatch, pre-execution validation opening, unaccepted quit) preserves `SelectIsActive`, the execution identity, and zero result-history entries; each finalizer (actual new execution start with fresh request identities, entering result history with future page dispatch refused, ending cancellation that settles first, first-page failure before rows, accepted quit with pending and with idle work) ends the SELECT exactly once with exactly one entry; settled validation without an execution start never finalizes; and late old-execution messages plus repeated finalizers cannot reactivate or mutate a finalized SELECT.
- `internal/ui/snapshot_finalize_once_test.go` — exactly-once snapshot outcomes: idle success, successful rows with count unavailable (rows preserved, never complete), partial page failure and failure after rows (failed outcome, reason, rows preserved), cancellation before rows (non-tabular Cancelled) and after rows (tabular cancelled outcome), first-page failure before rows (non-tabular error); ascending rows and Issue #33 metadata captured immutably against later cache merges; repeated history-entry commands, late old-execution count/page results, replayed cancelled settlements, and repeated quit cleanup creating no second entry; and quit finalization per execution rather than per request.
- `internal/history/result_entry_test.go` — `AppendFinalized` exactly-once idempotence per execution ID with the first entry untouched, zero-execution rejection, retained-entry immutability against appended-fixture mutation and returned-copy mutation (columns, values, BLOB bytes, metadata), non-tabular Cancelled/error entry shapes with reasons and typed outcomes, and `FactsFromCache` snapshot independence from later cache merges.

- `internal/history/result_history_store_test.go` — Issue #36 bounded retention matrix (under/at/over capacity) proving exactly the 20 newest entries survive chronologically with stable non-positional IDs and oldest-first eviction preserving surviving IDs; deep immutability against source and retrieved-copy mutation including exact BLOB bytes, ascending absolute positions, and metadata; non-tabular error-entry retention; and the selection-primitive matrix (empty-store determinism, oldest/newest, boundary no-ops, evicted and never-allocated ID refusal, deep-copied returns).
- `internal/history/result_history_eviction_test.go` — defensive external-append eviction matrix over full and partially filled histories with alternating tabular/error entries: exactly the excess oldest entries unresolvable, every surviving ID intact, and the deterministic new-oldest reconciliation target.
- `internal/ui/result_history_browse_test.go` — pure `projectHistoryEntry` reslicing table (capacity vs snapshot size, absolute offsets from retained-range metadata, non-tabular error/cancelled projections, stored-entry preservation) plus the zero-refetch browsing walk: entry selection, repeated navigation with boundaries and the newest-boundary exit, resize reslicing from the same snapshot, and rendering — zero select/page/count executor invocations throughout.
- `internal/ui/result_history_ui_test.go` — the key seam (Ctrl+E/Y entry at the newest entry, older/newer traversal through tabular and error snapshots, boundary no-ops, Ctrl+Y-at-newest and Esc exit), `ExecutionStartedMsg` exiting result history and clearing stale selected rows before the execution finalizes exactly once, ordinary first-page error finalizing one error entry that replaces the result area with Esc dismissal and older-entry reachability, later-page failure finalizing a tabular failed snapshot, `database is locked` classified ordinary with no terminal state, and terminal-health override of the lock error.
- `internal/ui/result_history_eviction_test.go` — scripted defensive eviction: new-oldest fallback with exactly `Previously viewed result was evicted from history`, kind-aware fallback projections resliced at the current height, unaffected selections left untouched, empty-history base return with no historical rows, and no evicted entry data rendered in any frame after resize, navigation, or dismissal — zero database requests throughout.

## Issue #35 stable query-history navigation and consecutive-suppression tests

Pure cursor coverage in `internal/history/query_cursor_test.go` (empty-store `Newest`/`Oldest`/`OlderThan`/`NewerThan` determinism, oldest/newest identity and navigation one entry at a time by stable ID, repeated oldest/newest boundary no-ops, direction reversal, evicted and never-allocated IDs refusing to navigate with surviving IDs unchanged, retrieval-copy independence, and no append or ID consumption while browsing, plus the A→A suppression and A→B→A retention regression), a rich-state `RestoreBuilder` round trip in `internal/querybuilder/restore_check_test.go`, and scripted model coverage in `internal/ui/query_history_navigation_test.go` (empty-store Ctrl+P/N no-ops, first entry selecting the newest INSERT/UPDATE/SELECT store at the newest boundary, Ctrl+P older / Ctrl+N newer walks with oldest-boundary no-ops and reversal, newest-boundary and Esc exit, byte-exact full-field restoration across SELECT/UPDATE/INSERT with restored/source/retrieved mutation proving retained immutability, SET and Limit editing appending nothing, and retained stores unchanged throughout) and `internal/ui/query_history_eviction_test.go` (execution start exiting history mode and appending the edited restored INSERT state under unchanged suppression, cross-history A→A suppression regression, forced external eviction falling back to the new oldest with the exact notice and surviving IDs preserved, empty-store base return with valid builder data, and normal execution never stranding an evicted selection) — see [query-history-navigation.md](query-history-navigation.md).

## Issue #43 commit-boundary cancellation and accepted-quit cleanup tests

- `internal/connection/commit_boundary_test.go` — barrier-seam coverage with the `writeLeaseHook` counting-interrupt seam: `TestWriteInterruptScopedToLeaseBeforeBoundary` (exactly one interrupt dispatched against the write's own leased connection while cancellable, lease reused only after cancelled settlement), `TestWriteNoInterruptDuringRollbackCleanup` (Cancel repeated while the write is held inside noncancellable rollback cleanup dispatches no second interrupt, leaves state and work unchanged, and preserves the cancelled/confirmed-rollback result), `TestWriteNoInterruptAfterBoundarySettlement` (Cancel after settlement issues nothing and settlement stays closed), plus the revised `TestWriteCancellationWinsAfterStatementSuccess` in `write_test.go` holding the after-statement/before-COMMIT decision — the atomic check still wins, now proving `beginning → executing → rollback-cleanup` phases with no committing announcement before the boundary.
- `internal/ui/commit_boundary_test.go` — barrier-held fake-executor model tests: `TestWriteCtrlWCancelsOnceBeforeBoundary` (Ctrl+W in executing dispatches the scoped cancellation exactly once and deduplicates repeated keys), `TestWriteCtrlWAfterBoundaryIsInertWithExactFeedback` (in both rollback-cleanup and committing, repeated Ctrl+W returns no command, cancels no context, mutates no phase/work, and shows exactly `Commit in progress; cancellation is no longer available`), and `TestWriteBoundaryResistsRegressionAndStaleIdentities` (regressed executing, duplicate boundary, and stale-execution phases cannot cross the boundary backward or re-enable cancellation; no replacement write starts while boundary work pends).
- `internal/ui/write_quit_settlement_test.go` — accepted-quit lifecycle: cancellable-phase quit requesting exactly one cancellation and waiting through rollback resolution; committing/rollback-cleanup quit issuing no interrupt and awaiting the existing operation; committed, failed, unresolved-rollback, and unresolved-commit outcomes all exiting only after settlement with exactly one finalization and no untouched/persistence overclaim; duplicate acceptance, stale settlement identities, declined-quit exact restoration with live Ctrl+W, and replacement-work prohibition during the wait.
- `internal/history/write_quit_settlement_test.go` — result-entry assertions: `AppendFinalized` retaining exactly one `KindWrite` entry per execution with the newest selection returning it and duplicates rejected; committed/cancelled/failed labels claiming `untouched` only after confirmed rollback and never claiming persistence for non-committed outcomes.

## Issue #42 transactional write execution and summaries tests

- `internal/connection/write_test.go` (unix build, real modernc SQLite) — barrier-seam coverage of `StartWrite`: `TestWriteCommitLifecycle` (table-driven over qualified/unqualified UPDATE, DELETE, and runnable INSERT proving exactly one pre-BEGIN identity check via the `identityChecks` counter, the `beginning→executing→committing` phase sequence with no second statement, actual statement `RowsAffected`, persisted state verified through an independent connection, settled state, and healthy lease reuse by a follow-up write), `TestWriteTriggerSideEffectsCommit` (audit-table trigger effects committed inside the same transaction, `RowsAffected` excluding trigger effects, and a per-firing trigger proving exactly one execution), `TestWriteConstraintFailureRollsBack` and `TestWriteTriggerFailureRollsBack` (failed writes preserving the native UNIQUE/RAISE cause, confirmed rollback, untouched persisted state, no committing phase, lease reuse), `TestWriteCancellationBeforeBegin`/`...DuringStatement` (barrier-held beginning/executing transitions with deterministic cancellation → cancelled outcome, confirmed rollback where a transaction existed, unchanged database, and reuse), and `TestWriteCancellationWinsAfterStatementSuccess` (the release-blocking pre-COMMIT rule: commit phase observed behind the barrier, cancellation requested there, atomic check wins → cancelled with confirmed rollback and untouched state, phases including rollback-cleanup after committing).
- `internal/history/write_summary_test.go` — the `WriteSummary` label matrix (committed UPDATE/DELETE rows-affected wording, committed INSERT rows-added wording with no rows-affected form, cancelled and failed labels with and without confirmed rollback making the untouched claim only when confirmed), and `AppendFinalized` write-entry coverage (exactly one immutable `KindWrite` entry per execution identity carrying executed standalone SQL, summary, and actual `RowsAffected`; duplicate/late finalization appending nothing; zero execution identity rejected).
- `internal/ui/write_feedback_test.go` — Issue #44 table-driven held-write fixtures over estimating, estimate cancellation, beginning, executing, cancellation-requested, rollback-cleanup, and committing: exact label rendering and resize persistence, `cancelling…` held through typed rollback cleanup, stale/duplicate/post-boundary phase-message identity guards, blocked actions consumed with exact feedback, no command, unchanged fake call counts and focus, phase-appropriate Enter hints (`<phase> — press Ctrl+W to cancel`) and the exact `Commit in progress; cancellation is no longer available` boundary feedback, Ctrl+W routing/dedupe/boundary per phase, the shared quit confirmation opened and exactly restored per phase, permitted horizontal movement staying local with the label intact, and the Issue #27 read regression (`Running…`, `loading next page…`, `Counting rows…`, `Count unavailable`, read `cancelling…`) — see [write-phase-feedback.md](write-phase-feedback.md).
- `internal/ui/write_lifecycle_test.go` — scripted `Update` coverage with the controllable fake `WriteExecutor`: `TestConfirmedUpdateLifecycle` (confirmation exits both histories first and appends the complete query state exactly once — consecutive-identical suppression proven against an identical restored seed — then runs the sole write with the retained rendered SQL and SET-then-WHERE params and finalizes exactly one `KindWrite` entry tied to the execution identity with executed SQL, actual `RowsAffected`, and the rows-affected label), `TestConfirmedInsertLifecycle` (INSERT dispatch via `ExecutionStartedMsg` appending once and writing `InsertSQL()`/`InsertParams()` with omitted columns excluded, settlement reporting rows added), `TestCancelledWriteRequiresConfirmedRollback` (untouched claim only after confirmed rollback), `TestFailedWriteSummaryPreservesCause` (verbatim constraint cause with confirmed rollback), `TestWriteMessagesAreIdempotent` (duplicate/late phases, re-delivered and stale settlement appending and starting nothing, one finalized entry, `writeFinalized` set), `TestPostBoundaryWriteIsNoncancellable` (committing phase retires cancellation ownership — Ctrl+W dispatches nothing — and settlement finalizes exactly one committed result), and `TestPreparationAppendsNothing` (preparation, pre-start settlement, and dismissal append nothing). Updated `destructive_prep_confirmation_test.go` now asserts the Issue #42 execution-start contract: confirmation appends exactly one query-history entry (complete state) and no result entry, and replayed messages append nothing further — see [transactional-writes.md](transactional-writes.md).
